One JSON file per student scenario: the student's input plus the expected status of every
requirement. Claude Code creates these before writing the engine; add one whenever an edge
case is found. File names describe the case, e.g. phd-two-distinct-groups.json.
