# Interpretation decisions

Every place where the handbook is ambiguous and a human chose a reading. Newest first.
Claude Code appends here after the DGS answers a question; the next DGS reads this before
changing anything.

Decisions marked **DGS** were answered directly by the DGS. Decisions marked **default
(DGS-delegated)** are readings the DGS delegated on 2026-08-31 ("all other questions follow
your default decision") — any of them can be revised by editing this table and asking Claude
Code to update the engine (each has a matching test scenario).

| date | question | decision | decided by | handbook § |
|---|---|---|---|---|
| 2026-09-12 (third) | Red-team F1: a non-CSE credit refused by the nine-credit allowance — lost, or still in the total? | Still in the total. §4.2/§3.2 scope the allowance to "the course requirement" ("Up to nine (9) credits … may be used to satisfy the course requirement"); a passed graduate credit the allowance refuses is still a credit "of courses and research" toward the 60/30 total. `allocate.ts` sends credit refused ONLY by the `noncse` cap to `totalOnly` (`overCapToTotal` on the allocation), the course line reads "counts … toward the total-credit requirement only — over the 9-credit non-CSE cap — the allowance limits regular-course credit, not the total", and the cap row lists it as "over the cap — count toward the total-credit requirement only". Supersedes the 2026-08-31 "earns nothing" default for this one cap; the below-60000 (4xxxx) allowance still refuses outright. | DGS ("fix F1–F4") | §3.2, §4.2 |
| 2026-09-12 (third) | Red-team F2: a student's group pick that strands a flexible §4.4.2 course. | A pick is a preference, not a rule. `matchDistinctGroups` honours pins while they cost nothing and otherwise takes the assignment that covers more groups (`pinsIgnored`, `bestAssignment`); the categories row publishes `groupAssignments`, and the page pre-fills an UNSET group from it and says so in a toast. A student's own pick is never overwritten (only blanks are filled), and can be changed next to the course. | DGS | §4.4.2 |
| 2026-09-12 (third) | Red-team F3: the §4.4 qualifier read "met" (with the file-the-form reminder) while fewer than nine regular ND credits were earned. | §4.2: "students are required to take at least 9 credits of regular courses at Notre Dame before taking the qualifying examination" — the nine-credit row is a condition of the umbrella. Its status joins the children's, the umbrella's detail names the shortfall, and the reminder appears only when the umbrella is genuinely met. | DGS | §4.2, §4.4 |
| 2026-09-12 (third) | Red-team F4: with "Undecided", both §3.4 rows demanded their milestone. | The route is read off the record (`inferMsOption`: CSE 68902 or an accepted project report → project; CSE 68901, readers' approval or a defense → thesis; both or neither → still undecided) and the page pre-fills the radio with a toast; the student can change it. While genuinely undecided the two rows are alternatives: either milestone satisfies both, and each says "Either route satisfies §3.4 — pick yours under Your standing." | DGS | §3.4 |
| 2026-09-12 (third) | General rule: when the app can see the best choice for the student, make it. | Every student choice the record can settle is pre-filled and announced in a pop-up notice ("chosen automatically to maximise requirement coverage"), leaving the control for the student to change; only blank choices are filled, never a student's own. Applied to the §4.4.2 group and the §3.4 route (`autoSelect` in `app.ts`); the entry term, bachelor's award term and prior-graduate-study fields were already inferred from transcripts. Not inferable, so still asked: the Ph.D. "counted toward" question for a degree-shared course. | DGS ("apply this generally") | — |
| 2026-09-12 (second) | The Courses tab names the reviewer per course: `adgs_approval` beside `dgs_approval`. | DGS: “This will be confusing to the people who maintain the spreadsheet. In the ExternalCourses sheet, I am differentiating between adgs and dgs so future DGS knows who makes what decision. I have applied the same change in the Courses sheet … It will be better if the app/course rule pages recognize these values and show the correct reviewer accordingly. Also, it is possible that, in the future, the decision authority in the MSCSE tab goes back to DGS, or maybe only some courses' decision authority go back to DGS.” The DGS had already set 52 rows' `counts_toward_mscse` to `adgs_approval`, and the parser — which accepted only yes/no/dgs_approval — was skipping those rows, which is why the 40000-level courses vanished from the course-rules page. AS BUILT: the parser accepts the fourth value; the reviewer a course line names comes from the CELL (`approverToken`), carried through the boundary rewrite as a token so a `dgs_approval` course on the MSCSE tab still says DGS; the program-level default (ADGS for the MSCSE) covers decisions the sheet does not name per course — unlisted courses, the review request's recipient, transfer recommendations not ruled in ExternalCourses; and the rendered page keeps engine text as written (`data-keep-dgs`) so the page pass cannot overwrite a per-course reviewer. The course-rules page shows “With DGS approval” / “With ADGS approval” per cell. The fixture and the sample CSV mirror the sheet (40000-level rows `adgs_approval` for the MSCSE). Supersedes the blanket reading of the previous row for course approvals; keeps it for everything else. | **DGS** (2026-09-12) | §3.2, §4.2 |
| 2026-09-12 | The ADGS decides for MSCSE students; the DGS for Ph.D. students. | DGS (2026-09-11, evening): “the ADGS will make decisions in the approval chain of MSCSE students. Regardless of which requirements are being decided, ADGS decides them for MSCSE students, and DGS decides them for PhD students. Revise the app, the course rule page, and the spreadsheet accordingly.” AS BUILT: one rule at the boundary rather than a hundred edited sentences — `decisionWording()` (src/engine/decider.ts) rewrites every standalone “DGS” to “ADGS” in what an MSCSE student reads: requirement details, course lines, warnings, track notes, the review request's reasons, both e-mails; and `applyDeciderRule()` does the same over the rendered page, skipping the regions that name both people or quote the handbook (the contact card, the notices, the glossary, the print header, the footer). Handbook quotes are never rewritten. The review request and the Grad Admin cc go to the degree's decider (`deciderContact()`): adingler@nd.edu for the MSCSE, tjung@nd.edu for the Ph.D. The glossary describes the ADGS on the MSCSE tab; the contact card says who decides for whom. The course-rules page's pill is “With approval” and its key says whose — the ADGS's for the MSCSE column, the DGS's for the Ph.D. column; the sheet's value stays `dgs_approval` (no schema change; the README tells the DGS what it means per column). Guards: tests/mscse-no-qualifier.test.ts (no standalone “DGS” reaches an MSCSE student; the Ph.D. never sees “ADGS”) and both e2e page sweeps. | **DGS** (2026-09-11) | §1, §3, §4, §5.2 |
| 2026-09-11 (fifteenth) | The Notre Dame upload row is “Current ND Unofficial MSCSE Transcript” / “Current ND Unofficial Ph.D. Transcript”. | DGS: “change the naming to ‘Current ND Unofficial MSCSE/PhD Transcript’” — the word “Current” says what the row is for, against the previous-transcript rows below it. One helper (`ndRowLabel()`) names the row and the two messages that point at it; the label in app.ts now uses it too rather than its own copy. Spelled “Ph.D.” to match every other string on the page. | **DGS** (2026-09-11) | — |
| 2026-09-11 (fourteenth) | The Ph.D. tab cites §3 only where it must. | DGS: “When students are in the PhD tab, unless necessary, Section 3 should not appear in the explanation … If necessary, section 3 may appear. For example, for ‘MS along the way’, Section 3.2 is necessary.” Swept the engine's output over all 68 Ph.D. cases and the rendered page: the engine emits §3 only in the §3.5 and §3.6 track notes, which are the tracks themselves; the page had three leaks — the full-time-terms legend (“§3.3/§4.3”), and the Grad Admin card and glossary listing “exam and defense forms (§3.4, §4.5–4.7)” — now the degree's own sections. The approvals row already cited §4.2/§5.2 to a Ph.D. student since the thirteenth row. Guard: the app e2e sweeps the Ph.D. page and allows §3 only in a §3.5/§3.6 note or an along-the-way line. | **DGS** (2026-09-11) | §3, §4 |
| 2026-09-11 (thirteenth) | Two more from the DGS's read of the MSCSE cases. | (1) “For any students, when previous undergraduate transcript shows as in-progress, reject the transcript and show a warning message that says a completed BS transcript is required.” The Previous Undergraduate row now refuses a transcript with any course lacking a final grade — an ND ‘Courses in progress’ block or an ungraded row from another university — with: “This undergraduate transcript is still in progress — it lists courses without a final grade. A completed bachelor's transcript is required here: upload it again once the degree is finished and every course has a grade.” No preview opens. The Master's and Ph.D. rows are unchanged (a master's in progress is a real case). (2) “If the student is on the MSCSE tab, in the explanations, do not show Section 4 which is exclusively for PhD” — the approvals row cited §3.2/§4.2/§5.2 to a master's student, and three attestation labels, two course lines and the advisor to-do said “(§3.2/§4.2)”. Every such string now cites the degree's own section, and the MSCSE guards (tests/mscse-no-qualifier.test.ts, e2e step 11) forbid any “§4” on that tab, not only §4.4. | **DGS** (2026-09-11) | §3.2, §4.2, §5.2 |
| 2026-09-11 (twelfth) | The transcript's own word for its credit system counts. | DGS: “Can you tell from the transcript whether a university is on a quarter system? If so, can you multiply 0.67 to the credits shown to the students?” Usually yes: quarter-system transcripts print it in their term headers (“Fall Quarter 2023”, “Autumn Qtr”) or a credits heading (“Quarter Units”). The parser now reads that word (`quarterSystem`) — nothing weaker, since a lone “Winter” term can be a January session on a semester calendar — the preview shows it as a ticked checkbox the student can correct, each added row carries `creditSystem = 'quarter'`, and the engine converts at the exact 2/3 the app already used for the sheet's `credit_system` cell (DGS 2026-09-08: keep the exact ratio; 4 quarter credits → 2.67), saying on the line “converted from the quarter system at 2/3 — your transcript says quarter terms; the DGS's ruling for the university can correct this”. The DGS's ExternalCourses `credit_system` cell still overrides whatever the transcript said. Tests: tests/external-transcript.test.ts (‘quarter-system detection’), tests/scenarios/phd-quarter-from-transcript.json. | **DGS** (2026-09-11) | §5.2 |
| 2026-09-11 (eleventh) | Graduate student status and §5.2, restated by the DGS — CONFIRMED, no change. | DGS: “Non-Notre Dame graduate courses cannot be transferred if the students did not have graduate student status when they took the courses. The obvious exceptions are the ND graduate courses taken by ND undergrads, which may be transferred even beyond the normal 24-credit cap, subject to all the constraints including the triple-counting ban.” Verified against the live sheet the same day: a Purdue graduate course dated in or before the bachelor's award term reads “taken as an undergraduate student — no transfer credit (§5.2)”; one taken with graduate status is a §5.2 candidate; twenty-seven credits of Notre Dame 60000-level coursework taken as an ND undergraduate count in full toward the 24 and the 60 while the §5.2 row reads “0 of 24 transfer credits counted” (they never draw the cap); and a course answered ‘both’ is barred by the three-degree rule. This is the 2026-09-06 award-term rule plus the Graduate School's 2026-09-10 answer, working together as built. | **DGS** (2026-09-11) | §5.2 |
| 2026-09-11 (tenth) | The app, not the student, decides which 4+1 courses apply to both degrees — SUPERSEDES the ninth row. | DGS: “let the top two CSE 40xxx courses count towards both BS and MSCSE. Do not let students choose what count to what — there will be too many selections. Just explain that two courses were chosen to double count towards both degrees. When the transcript has 60xxx courses, try to save them for the graduate degree … the wording should be clear enough for students to know that courses WILL apply to both BS & MSCSE or WILL apply to only MSCSE, instead of ‘already counted’.” AS BUILT, MSCSE tab only: no dropdown. §3.5's shared allowance (`ms_bs_double_count_credits_max`, 6) is filled by the app from the student's 40000-level CSE courses that can count — best grade first, then earliest — and only if those leave room, from 60000-level courses inside §3.5's window, earliest first; everything else applies to the MSCSE alone. Each line says which: “will apply to both your bachelor's degree and your MSCSE — one of the courses chosen for §3.5's shared credits” or “will apply to your MSCSE only”, and the §3.5 note names the chosen courses and the rule. The chosen courses fill their caps before any other course in their tier, so the choice and §3.2's six-credit allowance agree (the term-order fill had handed the allowance to a weaker, earlier course). A `countedToward` value in a saved record is ignored on this tab; the Ph.D. tab keeps its three-degree question. Tests: tests/scenarios/mscse-4plus1-app-chooses.json, mscse-shared-with-bs.json, mscse-prior-nd-undergrad-4xxxx.json, tests/engine-units.test.ts (‘an MSCSE student's own undergraduate transcript’); e2e step 10 asserts no dropdown and the two wordings. | **DGS** (2026-09-11) | §3.2, §3.5 |
| 2026-09-11 (ninth) | A 4+1 student's undergraduate 60xxx courses have TWO answers, not four. | DGS: “those courses either count only to MSCSE or count to both BS & MSCSE, subject to the 6-credit limit. ‘count to neither’ or no answer are not options. Students have to choose one of the two options above, and the app should apply the 6-credit limit (if students chose to count 2 60xxx courses to both degrees, other 60xxx courses can only be applied to the MSCSE).” AS BUILT, for the MSCSE tab: the dropdown offers “Only my MSCSE” and “Both my bachelor's degree and my MSCSE” behind a “Choose…” placeholder; nothing counts until one is chosen (the line reads “not counted yet — choose, next to the course, whether it counts only toward your MSCSE or toward both…”); ‘both’ draws §3.5's six shared credits (`ms_bs_double_count_credits_max`) and a third shared course is over that cap; ‘MSCSE only’ counts in full and touches no cap. The same two answers cover 40000-level undergraduate coursework, which still needs its sheet approval. Records saved under the earlier four-way question still load: ‘bs’ reads as both, ‘neither’ as MSCSE only. The Ph.D. tab keeps its three-degree question unchanged. Tests: tests/scenarios/mscse-4plus1-two-answers.json; scripts/e2e/drive-transcript.mjs step 10. | **DGS** (2026-09-11) | §3.5 |
| 2026-09-11 (eighth) | Five readings of §4 the sixty-eight invented Ph.D. cases forced, ruled by the DGS. | (8) “At least nine credits at Notre Dame” means REGULAR courses: “the 9 credits at ND should be regular classes.” The row already counted only the regular pool; its title now says so (“At least 9 credits of regular courses taken at Notre Dame”), so a student with 18 research credits at Notre Dame reading “3 of 9” knows why. (9a) §4.4.1 core knowledge is satisfied by a course the rules sheet explicitly approves for a core area — the Courses tab's `core_area` for Notre Dame courses, the ExternalCourses tab's `satisfies_core_area` for others — whether or not that course's credit transfers, and a title keyword is only ever “pending review”; that is what the app did, and it is now recorded. §4.4.2 is satisfied by Notre Dame courses “whether during BS, MSCSE, or PhD, even though the courses do not count towards the credit requirements of PhD”: a prior Notre Dame course no longer waits for its §5.2 transfer, and is not dropped for being over the §5.2 cap, outside the five-year window or below the transfer grade floor — §4.4.2's own B floor still applies (SUPERSEDES the 2026-09-09 reading that tied §4.4.2 to the credit transferring). (9b) The student's “transfer approved” checkbox settles only courses the DGS has reviewed — an ExternalCourses row with `transferable` set, or a Notre Dame course the Courses tab lists; a never-reviewed course stays pending whatever is ticked, its line says the checkbox cannot apply to it yet, and the §5.2 row is “met” only when nothing is pending. (9c) A master's project or thesis (course_type project, or CSE 68901/68902) “is not a regular course. It cannot be transferred, so it should not count toward PhD” — a prior CSE 68902 used to draw six of the twenty-four and read “counts toward the project/thesis requirement” on a Ph.D. report. (9d) A Ph.D. student entering in a summer session is treated as entering the following fall — the app's existing normalisation (decision Q17c), now confirmed for the 18-month research clock too. Tests: tests/scenarios/phd-nd-masters-transfer-approved.json, phd-transfer-unreviewed-attested.json. | **DGS** (2026-09-11) | §4.2, §4.4.1, §4.4.2, §4.4.3, §5.2 |
| 2026-09-11 (seventh) | Seven defects the sixty-eight invented Ph.D. cases turned up, fixed on the DGS's word. | (1) The review request kept asking for a §5.2 recommendation the DGS had already given: the attestation guard covered only ExternalCourses rulings, and Notre Dame's own courses never have one — every ND-MSCSE-then-Ph.D. student was told to email the DGS about approved transfers; and it listed senior-year 60xxx coursework that is not transfer credit at all. review.ts now treats a listed Notre Dame course as reviewed and skips coursework that counts without the transfer cap. (2) Non-CSE 60xxx coursework taken as a Notre Dame undergraduate (twelve credits of EE on an EE bachelor's) touched no cap and no checkbox; it now draws the nine-credit non-CSE allowance and needs the same advisor + DGS approval a non-CSE course taken in the program needs. (3) Residency counted registered credits from rows the audit itself rejects — a ‘W’ row, an exact same-term duplicate — turning six-credit semesters into full-time ones; `fullTimeTermRecords` reads the classified list now. (4) Course ids are read case- and space-insensitively (`canonicalCourseId`: “cse60641” → “CSE 60641”) everywhere the sheet is consulted and on entry, so a hand-typed or OCR-read id no longer lands under the non-CSE allowance; a number with placeholder letters (“CSE 6xxxx”) is warned about and counted only once completed. (5) The over-cap line names the cap that actually ran out, not every cap the course draws on (“over the transfer-credit cap and 9-credit non-CSE cap” with fifteen transfer credits free). (6) A university with no ExternalCourses row has no credit system on record; the pending line now says the credits are shown as printed and that the DGS's ruling converts quarter hours — and under (9b) such credits no longer count until that ruling exists. (7) Wording: the §5.2 row no longer repeats undergraduate ND lines, says “0 counted; N pending” instead of “24 of 24 counted” while pending, names “a student with no prior graduate degree” rather than “a prior program that was not completed”, and no longer says “over the cap” for a candidate the DGS has not picked; a “not counted —” line is never painted green (superseding the 2026-09-06 mark rule — the core area has its own row); after a granted extension the §4.4 chip reads “Was due by … — extended by the DGS” instead of “upcoming” for a past date; the Ph.D.'s §3.5 note promises the degrees question only to a student who holds an ND master's; a hand-typed transfer with no university is warned about. Tests: tests/scenarios/phd-ee-undergrad-noncse-cap.json, phd-residency-ignores-rejected-rows.json, plus the six transfer scenarios re-pinned to the new wording. | **DGS** (2026-09-11, from the Ph.D. corner-case run) | §4.2, §4.3, §4.4, §5.2 |
| 2026-09-11 (sixth) | §5.2's five-year window binds the MSCSE too. | DGS: “The five-year transfer window apply to MSCSE as well.” §5.2 criterion 3 — “the courses were completed within a five-year period prior to admission to a graduate degree program at Notre Dame or while enrolled in a graduate degree program at Notre Dame” — names no degree, but only `phd_transfer_window_years` existed, so the check simply did not run for a master's student and their decade-old coursework was never refused on age. NEW PARAMETERS KEY `ms_transfer_window_years` = 5 (§3.2), added to the live sheet with the Ph.D.'s key left as it was: one key per degree, so the two can differ if the Graduate School ever distinguishes them. Sheet schema change approved by the DGS in the same message; data/README.md, the sample CSV, the test fixture and KNOWN_PARAMETER_KEYS all carry it, and the snapshot was re-synced (0 errors). Verified against the live sheet: a 2019 course for a Fall 2026 entrant now reads “not counted — completed outside the 5-year window before admission (five-year window, §5.2)” and the new ms.transfer row counts 3 of 9. Tests: tests/scenarios/mscse-transfer-window.json. | **DGS** (2026-09-11) | §3.2, §5.2 |
| 2026-09-11 (fifth) | Four things the sixty-eight invented MSCSE corner cases turned up. | (1) CSE 68901 DID NOT SATISFY §3.2's six project/thesis credits. The Courses tab types it `course_type = research`, so a thesis-option student's credits landed in the total-only pool and the row read “0 of 6 credits complete. Register for CSE 68902 (project) or CSE 68901 (thesis direction)” — refusing the course and then recommending it. DGS: “change the rule so that students can take 6 credits of CSE 68902 or 6 credits of CSE 68901. Ignore the course type.” So for the MSCSE the course ID now decides the project pool, whatever the sheet's cell says — §3.2 names both numbers in its own sentence — and six credits of either, or a mix of the two, satisfies the row. The Ph.D. is untouched: §3.2 is the master's section and a Ph.D. student's thesis-direction credits are research credits. No test could catch the original defect because tests/fixtures/rules/courses.csv typed CSE 68901 as `project` while the live sheet typed it `research`; the fixture now mirrors the sheet, so mscse-thesis-complete.json passes only because of this rule. Tests: tests/scenarios/mscse-thesis-direction.json. (2) A COURSE ENTERED WITH 0 CREDITS said only “not counted”, with no reason, no warning and nothing pointing at the credits field — and a blank credit-hours column is an ordinary transcript-parse artifact. DGS: “allow 0 credits for courses with grades, but show a warning message and double-check with the students [when] they attempt to add 0-credit courses.” The row is kept and counts nothing, which is what 0 credits means; its line now reads “not counted — entered with 0 credits; check the credit hours on your transcript”, a warning names the course, and both the manual form and the transcript preview ask before adding one (`confirmDialog` in copy-dialog.ts, the safe answer focused, Escape cancels). Tests: tests/scenarios/mscse-zero-credit-course.json, drive-app.mjs. (3) THE MSCSE HAD NO §5.2 TRANSFER ROW. The cap was enforced — a fourth transferred course read “over the transfer-credit cap” — but nothing said what the cap was, so a student could only find it by exceeding it. The Ph.D.'s own row moved to src/engine/requirements/transfer.ts and both degrees now use it, the master's with `ms_transfer_completed_ms_credits_max` (9) or `transfer_unfinished_ms_credits_max` (6). New id `ms.transfer`. Tests: tests/scenarios/mscse-transfer-cap.json. (4) TWO SENTENCES. A row that is MET no longer carries its “Register for…” advice (`extraDetail` is now added only while the row is unmet) — a finished student was being told to register for the course they had just finished. And a course entered twice in the SAME term — what a hand-typed course plus an import of the same transcript produces — said “the Fall 2025 attempt of this course is the one counted”, naming the term the student is already looking at and identifying neither row; it now reads “entered twice for Fall 2025 — credits count once (§4.4.2), so this duplicate row counts nothing. Remove it if it is not a second registration.” A genuine retake in a later term keeps the old sentence, which was right. | **DGS** (2026-09-11, from the corner-case run) | §3.2, §3.4, §5.2 |
| 2026-09-11 (fourth) | §3.5 has a WINDOW, and it binds the MSCSE. | DGS, correcting the reading reported earlier the same day: “60xxx courses taken in junior year 1st semester should not count toward MSCSE. Only courses taken in 2nd semester of junior year and both semesters in senior year should count.” §3.5's own sentence says as much — “one or two 3-credit CSE courses at the 6xxxx level in the second semester of the junior year and the senior year” — and the app had been counting a graduate course from ANY undergraduate term, because the Graduate School's 2026-09-10 answer names no term. AS BUILT: for an MSCSE student, Notre Dame coursework at the 60000 level and above taken as an undergraduate counts only in the three fall/spring semesters ending with the one the bachelor's degree was awarded in (`semesterNumber(bachelorsAwarded, term) >= -1`); earlier than that it reads “not counted — taken before the second semester of your junior year, which is where §3.5 begins…”. A summer term reports the preceding semester, so the summer between the junior and senior years is inside the window. The award term is what places a course in the student's academic years, so without it nothing can be placed: the line then asks for it rather than guessing. THREE deliberate boundaries: the Ph.D. keeps every term (the Graduate School's answer is the Ph.D.'s rule and names none); 40000-level coursework has no window either, since its allowance is §3.2's and the BS/MSCSE double count names no term; and a December graduate's window is still the three semesters ending at graduation, which for them is the junior fall rather than the junior spring. The window is §3.5's shape, not a tunable, so it is in code with the handbook sentence quoted — say the word and it becomes a Parameters key. Tests: tests/engine-units.test.ts (‘§3.5's window on graduate coursework taken as an undergraduate’). | **DGS** (2026-09-11) | §3.2, §3.5 |
| 2026-09-11 (third) | The MSCSE hears nothing about the Ph.D. qualifying examination. | DGS: “Core knowledge requirement and Specialization category requirement are not relevant to MSCSE. Anything related to them should not be shown to current MSCSE students who are in the MSCSE tab.” They are §4.4.1 and §4.4.2, components of §4.4, which the MSCSE does not have. AS BUILT: the course lines’ core note is empty for an MSCSE student (allocate.ts), and an undergraduate course from another university reads only “not counted — taken as an undergraduate student, so it brings no transfer credit (§5.2)”; the review request never asks a core-area question and never names §4.4.1 (review.ts); a core-sounding TITLE is no longer a reason to offer, keep or list an undergraduate row — in the transcript preview, the Notre Dame preview’s ticking, or the coursework table — so for an MSCSE student only Notre Dame coursework that can actually count is shown; and every note, tooltip and hint that explained undergraduate coursework by what §4.4.1 allows now has an MSCSE wording. Two further leaks the sweep found and fixed, both wrong for an MSCSE student well beyond the qualifier: the “Ask the Grad Admin to process” card listed the qualifier form (§4.4), §4.5–4.7 and the MSCSE along the way, and the entry-term hint counted the §4.3 eight-year limit, the §4.4.3 research qualifier, the qualifier’s four semesters and the OCE deadline. Guarded two ways, because the rule is about EVERY surface: tests/mscse-no-qualifier.test.ts asserts that no requirement row, course line, warning, track note, review-request line or either e-mail matches /§4.4|core.knowledge|core area|specialization|qualifying examination|qualifier/i for an MSCSE student (and that a Ph.D. student still matches all of it, so the guard cannot go vacuous), and scripts/e2e/drive-transcript.mjs step 11 asserts the same over the whole rendered page and over an external transcript’s preview. The public course-rules page already hid both columns in its MSCSE view. | **DGS** (2026-09-11) | §3.2, §4.4.1, §4.4.2 |
| 2026-09-11 (second) | What the import does for an MSCSE student who uploads their own Notre Dame UNDERGRADUATE transcript, and what the Notre Dame row is called. | Two asks, one correction. (1) NAME: the row was “ND Unofficial Transcript” whichever tab the student picked; it now follows the tab — “ND Unofficial MSCSE Transcript” or “ND Unofficial Ph.D. Transcript” (`ndRowLabel()`, used by the row and by the two messages that point at it), because a 4+1 student holds several Notre Dame transcripts and the row wants the one for the program they are in now. (2) 40000-LEVEL COURSEWORK: the DGS first asked that up to two 40xxx courses “should count toward the MSCSE degree”, then corrected it the same hour — “they ‘may’ count, subject to all other constraints, so they should be listed when a MSCSE uploads an ND undergrad transcript for further decisions & review.” AS BUILT, on the correction: such a course is never counted silently. The student is still asked which degrees it has already counted toward (the question is the MSCSE's own — two degrees, not three — and the dropdown offers only the two answers that can be true); the sheet row is then read exactly as it is for a course taken IN the program, so `counts_toward_mscse = dgs_approval` — which is what nearly every 40000-level row says — makes it count PROVISIONALLY, amber, inside §3.2's six-credit allowance and §3.5's six credits shared with the bachelor's degree, and puts it in the “Ask the DGS to review” card until the approval exists. The existing “the DGS approved my course(s) below the 60000 level” attestation is what settles it. `priorNdShape()` now carries that verdict for BOTH programs, so a prior Notre Dame course flagged `dgs_approval` no longer counts unasked for a Ph.D. student either. (3) THREE SURFACES WERE HIDING IT: the transcript preview, the coursework table and the review card each kept their own copy of “can this undergraduate row matter?”, all of them “only a §4.4.1 core-keyword title” — so CSE 40166 was read from the transcript, dropped from the preview, and (when added by hand) counted by the report while invisible in the table. They now ask one exported engine predicate, `priorNdUndergraduateCanCount()`. Their notes were rewritten to match: an ND undergraduate transcript no longer tells the student that undergraduate coursework can only demonstrate a core area. (4) The §3.5 track note still described the pre-2026-09-10 rule (“does not count the courses toward the MSCSE”, “the department is asking the Graduate School”) and contradicted the lines below it; both versions rewritten. Tests: tests/scenarios/mscse-prior-nd-undergrad-4xxxx.json, tests/engine-units.test.ts (‘an MSCSE student’s own undergraduate transcript’, ‘priorNdUndergraduateCanCount’), tests/review-pending.test.ts, and scripts/e2e/drive-transcript.mjs step 10, which drives the real import with a new ND-undergraduate fixture. | **DGS** (2026-09-11, corrected the same day) | §3.2, §3.5, §4.2 |
| 2026-09-11 | Where a 4+1 student's transcripts go, when the degrees come on TWO of them. | The note above the four upload rows was written for one combined PDF, which the DGS reports is not the common shape: "ND's 4+1 program gives students two separate transcripts. One for BS and the other for MSCSE. It seems there are institutions that do this." Rewritten to name both shapes and say where each goes — two transcripts, one per row; one transcript covering both degrees, the Master's row once and never the same PDF twice — and to say that Notre Dame's own transcripts belong in those rows for a degree already finished, since the ND row is for the program the student is in now. The "Taken as" explanation stays, because it is what decides whether a course brings credit. Pinned by scripts/e2e/drive-transcript.mjs. | **DGS** (2026-09-11) | §3.5, §5.2 |
| 2026-09-10 (evening, second) | The GRADUATE SCHOOL answered the §5.2 question, and answered far more than was asked. | Verbatim, through the DGS: "Any 60000-level and above coursework taken as an undergraduate, not being used to fulfill undergraduate degree requirements can be used to satisfy both the master's and the PhD. Notably, such credits are counted towards the PhD, even those above and beyond the usual 24 allowed for transfer. The only hard constraint is that the same course's credits cannot count towards three degrees (BS, MSCSE, PhD) at the same time. … up to two 40000-level courses taken by ND undergraduates can count towards both BS and MSCSE. … an ND 4+1 student can have up to 6 credits (whether 40xxx or 60xxx courses) counted towards both degrees. … up to 6 credits from 40xxx courses can count towards PhD." AS BUILT: Notre Dame coursework taken as an undergraduate is no longer §5.2 transfer credit at all — it never touches that cap or that row. At the 60000 level and above it counts in full toward the 24 regular credits and the 60 (the DGS confirmed, asked directly, that this holds whether or not the B.S. used the course); below it, inside §4.2's existing six-credit allowance, the same six a course taken in the program would use (the DGS confirmed it is one allowance, not two); below the 40000 level, or non-CSE below 60000, nothing — those courses keep the line they have always had, which leads with the §4.4.1 area they may still demonstrate. The three-degree bar is the Ph.D.'s alone. The app cannot know which degrees a course was spent on, so it ASKS THE STUDENT per course (`CourseEntry.countedToward`: bs / mscse / both / neither) — and only where the answer can change something: Notre Dame undergraduate coursework, for a student who holds a Notre Dame master's. Unanswered counts nothing and says so, in amber. A Ph.D. student with no Notre Dame master's is never asked, since nothing of theirs can already have counted toward two degrees. On the MSCSE side the DGS asked for §3.5's own limit to be enforced too: coursework shared with the bachelor's is capped at `ms_bs_double_count_credits_max` (6). Tests: tests/scenarios/phd-four-plus-one.json, phd-nd-bachelors-then-phd.json, phd-nd-grad-before-bachelors.json, mscse-shared-with-bs.json, tests/engine-units.test.ts ('undergraduate Notre Dame coursework'). | **Graduate School**, relayed and extended by the **DGS** (2026-09-10) | §3.5, §4.2, §4.4.1, §4.4.2, §5.2 |
| 2026-09-10 (evening) | The DGS took the §5.2 concern to the Graduate School and, until it answers, reversed the ruling made earlier the same day. | STRICT §5.2 for now: no credit earned before the bachelor's degree transfers into the Ph.D. — not even §3.5's junior/senior-year 6xxxx courses, which the department counts toward the MSCSE. Only fifth-year (post-B.S.) coursework transfers. DGS: "You were right. It's the graduate school's rule, not the department. I am inquiring about this. Until then, let's strictly follow the graduate school's original wording and allow no pre-BS credits to be transferred." NEW, and NOT a return to the 2026-09-06 state: those courses now satisfy BOTH components of the qualifying examination — §4.4.1 core knowledge as before, and §4.4.2 specialization, which they could not do while §4.4.2 was tied to the credit transferring (2026-09-09). "They can still be used to satisfy the core knowledge and specialization category requirements in the qualifying exam requirement." Neither component is credit, so nothing about §5.2 is in the way. Withdrawn with the reversal: the `seniorgrad` cap, the `phd_senior_grad_credits_max` parameter (removed from the live sheet too), the cap row and the fill-order priority — all restorable from the commit that added them if the Graduate School allows it. The open question is tracked at the TOP of docs/STATE.md, and the DGS asked to be reminded of it every session until it is resolved. | **DGS** (2026-09-10, evening) | §3.5, §4.4.1, §4.4.2, §5.2 |
| 2026-09-10 | May a §3.5 course — one of the one or two 6xxxx courses a 4+1 student takes in the junior/senior year and counts toward the MSCSE — transfer into our own Ph.D., even though it predates the bachelor's degree? | YES, up to §3.5's own allowance, for NOTRE DAME's 4+1 only. DGS: "up to 2 60xxx level courses taken in the senior year can be used to count toward MSCSE degree requirement … up to two 60xxx level courses can be transferred to satisfy both 24-credit regular course requirement and the 60-credit requirement. Then, they can satisfy the qualifier category requirement." This SUPERSEDES the 2026-09-06 ruling for those courses; everywhere else the bachelor's award term is still absolute, because §5.2 criterion 2 asks for graduate student status and §3.5 is about this department's own program. The limit is the Parameters key `phd_senior_grad_credits_max` (6 = two 3-credit courses), allocated inside §5.2's own cap, and the courses still need the DGS's recommendation like any transfer. Where a student has more §3.5 courses than the allowance, the DGS chose the order: "prioritize courses that can satisfy the qualifier examination requirements … Among the courses that satisfy the qualifier examination requirements, prioritize the courses that will satisfy more requirements" — so a course carrying both a §4.4.1 core area and a §4.4.2 group fills it before one carrying either, and before one carrying neither. FLAGGED to the DGS when they ruled: criterion 2 is the Graduate School's, not the department's, so an individual case may still be questioned there; docs/HANDBOOK-REVISIONS.md §4 carries it. Tests: tests/scenarios/phd-35-allowance-priority.json, phd-four-plus-one.json, tests/engine-units.test.ts ('§3.5 allowance boundaries'). | **DGS** (ruled 2026-09-10) | §3.5, §4.2, §4.4.2, §5.2 |
| 2026-09-10 | The entry term on a 4+1's transcript. | DGS: "for students who have finished the 4+1 program at CSE ND, if the student advances to our own PhD, the entry term is when they start the PhD." A 4+1 leaves a signature nothing else does — graduate-level terms that BEGIN BEFORE the bachelor's degree — so where that holds, the entry term is the first graduate term after the LAST degree the transcript awards, not "the first graduate-level term" (which is the student's third year of college). The same rule serves a 4+1 still in the MSCSE: their last degree is the bachelor's, and the term after it is the master's year. An MSCSE earned ALONG THE WAY (§4.5) looks similar — a master's awarded with graduate terms after it — but its graduate terms all start after the bachelor's degree, so the earlier reading and its `alternative` are unchanged there. A stated admit term still wins over everything. Tests: tests/transcript.test.ts. | **DGS** (ruled 2026-09-10) | §3.5, §4.3 |
| 2026-09-10 | A 4+1 student's audit was right when their record was built by hand and wrong on the path they actually take: import the transcript, then correct the entry term. | Everything that turns on the ENTRY TERM is now worked out again when the student corrects it, not once at import. The chain that was broken: the import reads the entry term as the first graduate-level term, which for a 4+1 is the §3.5 senior-year course — two years early; the Notre Dame master's, awarded after that term, therefore did not look "already held", so `ndMasters` went unset; correcting the entry term then re-filed the coursework and set "Prior graduate study" to UNFINISHED, halving the §5.2 cap from 24 to 6, so most of a completed MSCSE read "over the transfer-credit cap", §4.4.2 fell to two groups, and the §4.5 along-the-way row came back for a student who holds the degree; and ticking "I already hold the MSCSE from Notre Dame" removed that row but left the cap at 6, because the checkbox never re-derived the value the cap reads. Fixes: `Student.ndDegrees` keeps the transcript's dated conferrals so the reading can be made again; `deriveNdMasters` re-reads it on every entry-term change, filling the field only while it is empty or still an import's own reading (the rule the bachelor's award term has followed since 2026-09-06); `derivePriorMs` applies "completed" whenever the Notre Dame master's is known rather than only from the untouched default; and the checkbox re-derives it. Verified end to end: after the correction the cap is 24, nine credits transfer, §4.4.2 is met and the §4.5 row is gone. | Claude Code (defect found by an adversarial sweep of the DGS's 4+1 question; the hand-built case had passed) | §3.5, §4.5, §5.2 |
| 2026-09-10 | The DGS's four answers on Ph.D. students required to take 50xxx Transition to Computing courses (§3.6). | (1) Only CSE 50502 counts toward the Ph.D. for now: CSE 50501 and CSE 50503 stay `counts_toward_phd = no`, so they count toward nothing, which is what the sheet already said. (2) Once the DGS's approval is recorded, CSE 50502 counts toward BOTH §4.2's twenty-four regular-course credits and the Graduate School's sixty — which is what the engine already did, and is now pinned; it still draws on §4.2's six-credit allowance for courses below the 60000 level, so a student who also has six credits of 4xxxx loses it to the cap. (3) The "talk to the DGS" note promised on 2026-08-31 for students on the §3.5 / §3.6 tracks is BUILT (src/engine/tracks.ts, pure). The app does not ask which track a student is on — it recognises §3.6 from a 50000-level CSE course and §3.5 from a graduate-level Notre Dame course dated in or before the bachelor's award term — and the note is program-specific, because the handbook says different things to an MSCSE and a Ph.D. student about the same course. It sits ABOVE the report's dial, styled as a note and not as a warning: no verdict changes, and nothing is wrong with the record. (4) The fourth §3.6.1 course, the CSE 60xxx Integrative Computing Studio, is not offered yet, so its absence from the Courses tab is correct. Also fixed the same day: the self-check page still showed the sheet's `notes` as a tooltip on the course name, against the 2026-09-09 decision — and to a Ph.D. student on a bridge course the note it showed was about the MSCSE. | **DGS** (answered 2026-09-10) | §3.5, §3.6, §3.6.1, §4.2 |
| 2026-09-10 | Checking how a Ph.D. student REQUIRED to take 50xxx Transition to Computing courses (§3.6) is audited, at the DGS's request. | The audit is right in the places that matter and one thing was broken. Working: §3.6.1 for the MSCSE comes straight from the sheet (`counts_toward_mscse = no` on all three, so "does not count toward the MSCSE"); §4.3 residence counts REGISTERED credits, so a twelve-credit bridge semester is a full-time semester even though none of it counts toward the degree; CSE 50502's `core_area = algorithms` reaches §4.4.1 ("Satisfied by CSE 50502"), which is the 2026-09-03 ruling working as intended; and the §4.4 deadline can be extended with the existing checkbox. BROKEN, and introduced by the 2026-09-09 cap change: the attestation "The DGS approved my course(s) below the 60000 level" only cleared level 4, so CSE 50502 — the one bridge course the sheet permits for a Ph.D. — could never be approved and stayed amber, in the review request and on the advisor's list for good. Fixed, with the level-5 `dgs_approval` shape now in the fixture (CSE 50120) and pinned. Also fixed: a 50000-level course said it "uses the 40000-level allowance" (the line now names the course's own level), and a student-facing string cited "decision Q19", a label no row in this file carries — it now cites §4.2. Also corrected: data/README.md told the DGS the `level` column was informational; it is load-bearing, and a mistyped level silently changes the audit. | **DGS** (asked for the check) | §3.6, §3.6.1, §4.2, §4.3, §4.4.1 |
| 2026-09-09 | §4.2 caps credits "taken from a department other than CSE" at nine wherever they were taken, but the app could not tell one department from another on a transcript it did not write — DGS: "CompSci, CompS, CS, CE, ECE, CSYE etc. all can mean CSE in fact". How should it know? | THE SHEET SAYS, in two places, and neither guesses. (1) A new Parameters key `cse_subject_codes` lists the subject codes that mean CSE on another university's transcript — today `CS; CSCI; COMPSCI; CSE; CMSC; EECS; CSYE`. A code the list does not name counts against the nine credits: the list is where the DGS says which codes mean CSE, so its silence is an answer. (2) A new ExternalCourses column `is_cse` (`yes`/`no`/blank) settles one course the code cannot — `ECE` is a computing department at one university and a circuits department at another — and it wins over the list. With the parameter missing, NO transferred course is placed inside or outside CSE and the allowance is not applied to transfers at all, which is exactly how the app behaved before; so nothing changes for a student until the sheet has answered. Notre Dame's own earlier courses keep being decided by their own subject (`deptOf === 'CSE'`), never by the list. Chosen over a per-course column alone (97 rows to fill, and one more question on every course added) and over a code list alone (blunt: ECE would have to be always CSE or never). Live sheet today: 97 rows, 7 universities, 12 subject codes — CS/COMPSCI/CSCI/CSYE cover 64 rows, ECE is 18, and INFO/GS/MENG/INTN/ENCP/TELE/HUM the rest. Tests: tests/scenarios/phd-noncse-transfer-cap.json, tests/engine-units.test.ts ('is a transferred course a CSE course?'). | **DGS** (chose the mechanism 2026-09-09) | §4.2, §5.2 |
| 2026-09-09 | Three defects in how a Ph.D. student's NON-CSE courses are audited (found checking that path end to end at the DGS's request; each reproduced by running the engine first). | (1) §4.4.1 core knowledge could not see a non-CSE Notre Dame course. The core row offers the DGS any course whose TITLE names a core area, gated on the classifier's `unknown` flag — which was set only on the unlisted-CSE branch. So "EE 60566 Advanced Computer Architecture" produced "No Computer Architecture course yet" while the same course from another university, and an unlisted CSE course with the same title, were both offered for review. §4.4.1 puts no department limit on a core course ("either at Notre Dame or at their previous institution"), and Notre Dame's architecture course may well be EE's. The unlisted non-CSE branch now sets `unknown` too, and the review request names both facts: "not in the course rules yet; non-CSE course — needs advisor + DGS approval (§3.2/§4.2)". (2) A cap the Parameters tab does not give a limit for behaved as ZERO: the cap row said "cannot evaluate" while every affected course was painted red "over the ?-credit non-CSE cap" and its credits vanished from the totals — against CLAUDE.md's "a missing parameter renders 'cannot evaluate', never a default". Such a course is now amber and says what is actually wrong: "the rules sheet does not say what the non-CSE cap is, so this course cannot be counted yet — ask the DGS to fill it in". Nothing is granted on a guess either way. (3) A passed course partly over a cap read "count 1 of 4 credits toward regular courses" — a rewrite meant for the "would count"/"will count" leads, which already end in the bare verb, firing on the only lead that must stay third-person. Tests: tests/engine-units.test.ts ('non-CSE courses'), tests/scenarios/phd-noncse-cap.json — the Ph.D. side of the nine-credit cap had no scenario at all until now; only the MSCSE's §3.2 cap did. | **DGS** (asked for the check; approved these three fixes) | §4.2, §4.4.1 |
| 2026-09-09 | A Ph.D. student who did their bachelor's — and sometimes their MSCSE — at Notre Dame. Does coursework from the earlier Notre Dame degree count toward §4.4.2 Category Specialization? | YES, once the credit actually transfers into the Ph.D. under §5.2 — the DGS's recommendation and the Graduate School's approval are what make an earlier course part of this degree. §4.4.2 names no institution and no term ("Students are required to take three category specialization courses from three distinct groups and pass them with a grade of B or higher"), unlike §4.4.1's "or have previously passed"; the transfer is therefore the test. A course whose transfer is not approved yet is NAMED on the row, never counted, and the row reads "needs DGS review" rather than "unmet" where the approval alone would complete it. A course over §5.2's credit cap does not count, because its credit does not transfer. Only Notre Dame's own courses can reach this row: the Courses tab is where §4.4.2 group tags live. Test: tests/scenarios/phd-nd-masters-then-phd.json. | **DGS** (answered 2026-09-09) | §4.4.2, §5.2 |
| 2026-09-09 | §5.2 criterion 3 allows a course "completed within a five-year period prior to admission to a graduate degree program at Notre Dame **or while enrolled in a graduate degree program at Notre Dame**". §4.2 allows "Courses from a M.S. degree earned at Notre Dame or another institution **within the last five years prior to admission**". Which governs an ND master's finished more than five years before Ph.D. entry? | The FIVE-YEAR WINDOW, for every institution including Notre Dame's own programs — the app is unchanged. The DGS chose the stricter of the two readings rather than have the app take the more generous one on its own. The tension between the two sentences is real and is written up for the handbook committee in docs/HANDBOOK-REVISIONS.md; if the Graduate School reads §5.2's second clause as controlling, this row is the one to revisit (the check is the `windowYears` branch in src/engine/allocate.ts). | **DGS** (answered 2026-09-09) | §4.2, §5.2 |
| 2026-09-09 | A Ph.D. student who ALREADY HOLDS the Notre Dame MSCSE — what should §4.5's "MSCSE along the way" row say? | Nothing: the row is left out of their report entirely. A degree cannot be earned twice, and the row was offering it to them and counting their credits from zero toward it. The app learns the fact from the Notre Dame transcript's degree-conferral lines — a master's or Ph.D. awarded BEFORE the entry term, since one awarded after it is the along-the-way award itself — and a checkbox under Your standing ("I already hold the MSCSE from Notre Dame") corrects it either way. Stored as `Student.ndMasters`; presence is the fact, the term is only for the wording. Nothing else changes: those courses are still §5.2 transfer candidates. Tests: tests/scenarios/phd-nd-masters-then-phd.json (`expectAbsent`), tests/prior-nd.test.ts, e2e drive-app.mjs. | **DGS** (answered 2026-09-09) | §4.5 |
| 2026-09-09 | Does `counts_toward_phd` = `yes` on a 40000- or 50000-level course exempt it from §4.2's six-credit cap? | NO — DGS: "The pre-approval for 40xxx or 50xxx courses only means they are pre-approved to count toward the Ph.D. They are still subject to all other constraints." A sheet cell is a permission, not an exemption. Both levels now draw on the SAME six credits (§4.2 / §3.2), so the cap row is titled "At most 6 credits from CSE courses below the 60000 level" and the attestation reads "The DGS approved my course(s) below the 60000 level". This supersedes decision Q19 for LISTED courses only: a 50000-level course the sheet does not list still counts nothing, since the permission has to come from the DGS. Non-CSE courses below the 60000 level still count nothing at all (2026-08-31). In today's live sheet this affects CSE 50502 (Transition to Data Structures & Algorithms, `dgs_approval`). Test: tests/scenarios/phd-below-60000-cap.json. | **DGS** (instruction 2026-09-09) | §4.2, §3.2 |
| 2026-09-09 | Six defects found while checking the two Notre-Dame-degree cases end to end (each reproduced by running the engine before it was fixed). | (1) Every transfer landed in the REGULAR pool, so a prior Notre Dame research or thesis course counted toward §4.2's twenty-four regular credits — which §4.2 excludes in as many words — and a prior 40000-level course never touched the six-credit cap. An earlier Notre Dame course now keeps its own Courses-tab verdict (course_type, level, counts_toward_*) on top of §5.2's cap; courses from other universities are unchanged, since the Courses tab describes Notre Dame's catalogue. (2) "Prior graduate study" was worked out only during a transcript import, so correcting the entry term afterwards left it at "none" and applied a 6-credit cap instead of 24; it now follows the coursework (`derivePriorMs`). (3) A 4+1's senior-year graduate course was read as a prior master's, putting "Prior M.S., not completed" into students' emails — a course dated in or before the bachelor's award term no longer counts as prior graduate study. (4) A hand-typed Notre Dame course dated before the entry term stayed program coursework for good, earning §4.2 credits, §4.3 residence and §4.4.2 specialization; the manual form now re-files it as the import does. (5) A Notre Dame transcript dropped into a "Previous …" slot left the student's IN-PROGRAM courses filed as transfer candidates; that path now re-files by the entry term too. (6) The §5.2 row said "needs DGS review" when every entered course was excluded on its own terms and there was nothing to ask for; it is now "not applicable" with a line saying so. Also: the §5.2 explanatory paragraph is no longer suppressed above a group of prior Notre Dame coursework — §5.2's last sentence covers it, and a student who never left Notre Dame is the most likely to assume their courses simply carry over. | Claude Code (defects; found verifying the DGS's two cases) | §4.2, §4.4.2, §5.2 |
| 2026-09-09 | On an iPhone the schedule cards' course titles ran past the card edge and dragged the whole course-rules page sideways (DGS report, iPhone 14 Pro / Safari). | The one-course-one-line rule those tables carry is a DESK rule: below 600 px the same table is a stack of cards, where there are no columns to line up and a title that cannot wrap simply overflows. `white-space` goes back to normal there. Measured at 393 px: the page's scroll width was 536 px against a 393 px viewport, and is 393 now. The e2e's no-sideways-scrolling check would not have caught it while the sheet's schedule columns were blank, so it now also fails if any schedule cell's right edge passes its card. Two e2e selectors had to be scoped while fixing it: the schedule tables carry the `course-rules` class for styling, so `table.course-rules` had started matching them too, and every row count on the page now says `.all-courses table.course-rules`. | **DGS** (bug report) | — |
| 2026-09-09 | The live ExternalCourses tab had eight duplicated (university, course) pairs — sixteen rows, three copies of some — because a student may take the same course several times (a master's project, thesis credits) and the review request emitted a paste-ready row per ATTEMPT. The last row wins, so the earlier rulings were shadowed. | The copied request now emits one row per COURSE, keyed the way the sheet itself matches (`normalizeUniversity` + `normalizeCourseId`, so "CS-59800" and "CS 59800" at one university are one course). The course DETAILS below still list every attempt — that is the evidence the DGS weighs, and the repetition stays visible there — and the tables say so when a repeat was folded away. Nothing changes in the student's own coursework list, where each attempt still counts its own credits. | **DGS** | — |
| 2026-09-09 | Whether a course from another university transfers can depend on the degree: the DGS split `transferable` into `transferable_PhD` and `transferable_MSCSE`, and added a fourth value, `adgs_approval`. | The engine resolves the ruling ONCE, in `classify()`, against the student's own program, and puts it on `ClassifiedCourse.transferable` — so nothing downstream has to know which column to read, and a Ph.D. ruling can never be applied to an MSCSE student. `dgs_approval` and `adgs_approval` are one case for now, on the DGS's instruction, and both are told to the student as "needs DGS approval" (the §5.2 card lists them under "Needs DGS approval", the review request says "transfer needs DGS approval (§5.2)"). The two words are kept apart in the type rather than collapsed at parse time, so the day the MSCSE message should name the ADGS instead, the sheet's own word is still there to key on. A sheet still carrying the single `transferable` column fills both programs, so nothing breaks mid-rename. | **DGS** | §5.2 |
| 2026-09-09 | The DGS's `notes` were readable by students, through a Notes column and a per-row disclosure on the course-rules page. | Gone from the page: no column, no button, no legend entry, and the notes no longer feed the hover text either. They are the DGS's working notes and stay in the sheet. In their place, hovering (or tabbing to) any course in the three card sections shows what its table row says — type, both degree-credit verdicts, core area, specialization, typically offered, DGS reviewed — in one small card positioned by script, so the schedule tables' scroll region cannot clip it. | **DGS** | — |
| 2026-09-09 | The course-rules filters read as a ragged wrap, and the free-text search box was taller than the selects beside it. | One grid of equal cells, `repeat(auto-fit, minmax(180px, 1fr))`, with every control the same 36 px box; the view picker spans the row above, and the two switches and Clear share a line below. Nothing was removed — every filter and both switches are still there. | **DGS** | — |
| 2026-09-09 | `offered_semester` named the semester the two schedule columns were written for. The DGS saw that one stamp could serve more generally — "as of which semester is the spreadsheet up-to-date" — and asked whether the schedule could still be driven from it. | Yes, and the logic did not change: if the sheet as a whole is current for FA26 then `offered_now` describes FA26 and `offered_next` describes SP27, which is exactly what the shift already assumed. The key is `current_semester`; `offered_semester` is still read, so a sheet that has not been renamed keeps working, and `current_semester` wins where both appear. The generalisation does cost something, and the docs say so: the stamp now means "the DGS says the sheet is current as of X" rather than "the schedule columns were written for X", so moving it forward for an unrelated edit, without revisiting those two columns, republishes an old schedule under a new semester's name — the one thing the key exists to prevent. The three cells are one edit. | **DGS** | — |
| 2026-09-09 | The schedule cards name their semesters from today's date while `offered_now` / `offered_next` are static, so a DGS who did not update them the week a semester turned over would have the page show last semester's courses under this semester's name (DGS's own concern). | The Parameters tab gains `offered_semester`, a display-only key naming the semester `offered_now` was written for, written as the code the pages already print — `FA26`, `SP27`, fall or spring only (DGS 2026-09-09: "I'm afraid people will make mistakes in filling the values", and four characters leave less room for one than "Fall 2026"; case, a space or a hyphen and a four-digit year are all forgiven, the long form still reads, a summer code is refused because a schedule cannot be dated to one, and anything else is reported). The parameter is read once during assembly so that its diagnostic reaches the self-check page's diagnostics card and `npm run sync-sheet` — the typed accessors only report when something asks, and only the course-rules page asks, which shows no diagnostics. `effective_term` takes the code too, so the sheet has one way of writing a semester. The page compares it with today: the same semester → both columns as recorded; the semester BEFORE → what was recorded as "next" IS this semester, so it moves across and "next" reads "Not released yet."; anything else, including a missing value → both cards say "Not released yet." and a line says why. This is the project's never-guess rule applied to a schedule: a late update makes the page show LESS than it could, never something false. Summer counts as the coming fall, since schedules are kept for fall and spring. The same shift drives the table filter, so the cards and the table can never disagree. `src/ui/schedule-terms.ts`, with a test matrix over the rollover, the summer and the missing value. | **DGS** | — |
| 2026-09-09 | The schedule columns needed to be visible, not only filterable, and the overview cards wrapped every course title onto two lines. | Two cards, "Offered this semester — Fall 2026" and "Offered next semester — Spring 2027", above the qualifier overview, since the schedule matters to MSCSE and Ph.D. students alike. Each lists its courses with every attribute the main table carries EXCEPT the three the DGS excluded — "Typically offered" (a pattern from past years, which these cards supersede), "DGS reviewed" (the * beside a course id says it) and the notes. A semester with no `yes` and no `no` anywhere reads "Not released yet." rather than showing an empty list, which would say "nothing runs". Card widths are content-driven now: `.ov-grid` is a flex row whose cards size to their longest course, so one course sits on one line wherever there is room; the schedule cards take the whole row until a window can fit both seven-column tables. On a phone the text wraps again, since the cards are full width there. | **DGS** | — |
| 2026-09-09 | The Courses tab gained `offered_now` and `offered_next` (`yes` / `no`), and the DGS wants the course-rules page to show which courses are running this semester and next — without adding two columns to the table. | One filter, "On the schedule", with "Offered this semester (Fall 2026)" and "Offered next semester (Spring 2027)" — the semesters are named from today's date at Notre Dame, and "next" is the next FALL or SPRING, since summer is not a graduate teaching term. Only a `yes` puts a course in the list: a blank cell means the sheet has not said, and is never read as "will not run". The control and its legend entry appear only once some course carries a value, so a page whose sheet is not filled in never shows a filter that could only return nothing. The filter is in the shared link like every other. `RuleCourse.offeredNow` / `.offeredNext`; nothing outside the course-rules page reads them, and the self-check is untouched. | **DGS** | — |
| 2026-09-09 | The page stopped at 1240 px and centred, so a wide monitor showed the cards at a fixed width with empty margins either side. | `#app` has no width cap: the page fills the window at any width, with its 20 px padding. The two columns grow together — the inputs column is `minmax(480px, 30%)` rather than a fixed 480 px, so it no longer stays put while the report column takes every extra pixel. Paragraphs inside a card keep a 100-character reading measure, so the tables, the course lists and the requirement rows use the room while a sentence does not run the width of the glass; that is the one deliberate limit, and it is easy to lift. The tablet and phone breakpoints (1120 px, 900 px, 600 px) are untouched, and the e2e now fails if either page stops filling a 2200 px window or if the inputs column stays at its minimum. | **DGS** | — |
| 2026-09-09 | The prior Ph.D. transcript upload had no "Bachelor's degree awarded" field, and on the previews that did have one the year box cut the last digit off under the number spinner. | The field is on EVERY graduate-slot preview now, Master's and Ph.D. alike: the award term is what decides which of a transcript's courses were taken with graduate student status (§5.2), so a prior Ph.D. record needs it as much as a Master's one. The bachelor's slot still has none — that transcript is the degree, and its conferral date is read from it. The year box was 58 px with a 13 px font, which fits four digits only if the spinner is not there; it is 66 px in the table's year column and 88 px in the bachelor's field, which is not in a column and can breathe. The e2e measures it. | **DGS** | §5.2 |
| 2026-09-08 | Johns Hopkins' unofficial transcript shows the university's name only in the letterhead IMAGE, so the parser left the institution blank and the import was unusable. | The text does carry the acronym — "JHU Degree and Date Conferred" — so `guessUniversity` gained a LAST-RESORT table, `NAME_ONLY_IN_IMAGE` in src/transcript/external.ts, mapping an acronym to the school's real name. It runs only after every attempt to read a name from the text has failed, so a transcript that spells its name out always wins, and the pattern is case-sensitive so a lower-case look-alike (a surname, a web address) cannot rename a transcript. The recorded name is "Johns Hopkins University", spelled as the university spells it; the ExternalCourses tab matches case-insensitively, so a row written in capitals still matches. Its dotted course code ("EN.601.433" — division, department, course) is read too, both as one token and in the two-cell layout; both dots are required, so "VOL.12" and "MAY.2025" are still not course codes. UC San Diego (DGS 2026-09-09) needed the same entry plus two more fixes: a page tiled with a "UNIVERSITY OF CALIFORNIA, SAN DIEGO" watermark was leaving a fragment ("UNIVERSITY") as the name, so a cell that is only a generic institution word, or that repeats one, is no longer a candidate; and the acronym is now tried BEFORE the weak "… College" pass, because that pass was picking up the school named in the transcript's "DEGREES AWARDED BY OTHER INSTITUTIONS" block. A name worked out from an acronym is marked as a guess: the preview pre-fills it but leaves the box editable and says where it came from, unlike a name printed as text, which stays locked. Reviewing the Johns Hopkins work also turned up three parser faults it would have hit: a degree heading printed on a line of its own ("JHU Degree and Date Conferred", values below) did not open the degree block, so a completed Master's read as not completed and the §5.2 cap fell from 24 to 6; a section number between the code and the title was read as the credits; and a January Intersession or a Winter quarter inherited the previous header's season, dating a course months late with the term locked against correction. Tests: tests/external-transcript.test.ts ("a university whose name is printed only as an image", "a transcript whose watermark looks like its name"). | **DGS** | — |
| 2026-09-08 | The ExternalCourses tab's `transferable` column held only `yes` and `no`, so a course the DGS is willing to transfer FOR SOME STUDENTS — one outside the usual CSE ground that serves a particular dissertation — had no honest value: `yes` pre-approved it for everyone and `no` shut it out. | A third value, `dgs_approval`, the same word the Courses tab uses. It is a decision about the COURSE and an open question about the STUDENT, so the engine treats it as a transfer candidate and says why: every surface names the open decision and stops there (DGS 2026-09-08, twice: first "that explanation can be shown only once in one place", then "the student doesn't need to know" — whether the course serves the student's research is settled between the advisor and the DGS). The review request says "transferability decided case by case (§5.2)", the sign-off row "transfer — decided case by case by the DGS (§5.2)", the §5.2 card lists the courses under "Decided case by case by the DGS", separate from "Pre-approved" and "Not yet reviewed", and the coursework line says only what it says for any candidate; the review request keeps it and tells the student what to write; the Grad Admin's processing request ignores it until the student attests that the approval came through. `yes` is for a course any student may transfer, `dgs_approval` for one where the answer depends on the student, and a blank cell still means nobody has looked. One wording rule came out of reviewing it: the page never calls the course "outside the usual CSE ground", because `satisfies_core_area` is a separate column and such a course may well have a confirmed §4.4.1 core area on the same line. Tests: tests/external-rules.test.ts ("transferable=dgs_approval → a candidate the DGS decides case by case"), tests/review-pending.test.ts (the request keeps it; hand-typed spellings), tests/grad-admin-request.test.ts (not processable until attested). | **DGS** | §5.2 |
| 2026-09-08 | The full §4.4.1 core-area and §4.4.2 group names made the dense parts of the page hard to read — "Data Science and Artificial Intelligence" did not fit the group dropdown beside a course, and a "Counts toward" cell ran to three lines. | Short forms are used in exactly ten places the DGS approved from screenshots. The CATEGORY names come from the sheet, so they are shortened at display time by `src/engine/short-names.ts` (OS, Alg, Comp Arch, HCC, Arch, DS/AI, Sys/Soft); "Notre Dame" → "ND" is not that function's business — those are hand-edited literals, one per message, because each needed its article and its e2e pin moved with it. The ten places: the specialization row's "still open" list and its course → group lines, the group picker and its hint, the "Counts toward" chips, the report's "Credits at ND" meter, the previous-transcript preview notes, the coursework group headings, the Transcripts card's "ND Unofficial Transcript" row and the two messages about the wrong file. Everything where the exact wording carries weight keeps the full name: course titles, the requirement cards' own titles, every handbook quote, the glossary that defines the terms, the whole course-rules page, the messages copied for the DGS, the advisor and the Grad Admin, and the masthead and copyright lines (the DGS's call on all three). The substitutions run longest-first so "Computer Architecture" cannot come out "Computer Arch", and the article changes with the name ("a Notre Dame transcript" → "an ND transcript"). | **DGS** | §4.4.1, §4.4.2 |
| 2026-09-08 | The course-rules page carried "Listed under every category" as a sixth specialization category — an overview card and a filter value — which read as a category a course could belong to. | Removed. A course the sheet marks `any` now appears in each of the five real category cards and matches each of them in the filter, and a note above the cards says that a course listed under more than one category can fill only ONE of them, never several (§4.4.2 asks for three courses from three DISTINCT categories). The table's Specialization column says the same in its own words: "Any one category (student picks)" and "X or Y (student picks one)". | **DGS** | §4.4.2 |
| 2026-09-08 | `category_group` held one value, so a course could satisfy exactly one §4.4.2 group or, with `any`, all five — there was no way to say a course fits two or three. | The cell may now name SEVERAL groups, separated by `;` `,` `/` `\|` or spaces and case-insensitive: `hcc;dsai` means the course may fill either group and the student picks which. `any` still expands to every group, `ineligible` still means none, blank still means undecided. `RuleCourse.categoryGroups` is a list (with `categoryIneligible` telling "not eligible" apart from "not decided"), so the engine's matcher — which always took a list of options per course — needed no change beyond reading it. A typo in one code is reported and dropped while the row's other codes keep working. The self-check shows the group picker whenever a course leaves a choice and offers ONLY that course's groups; the course-rules page labels a two-group course "Human Centered Computing or Data Science and Artificial Intelligence (student picks)", matches it under either group in the filter, and lists it in both overview cards. | **DGS** | §4.4.2 |
| 2026-09-08 | The requirement names in a course's "Counts toward" cell were too long to read across a narrow column. | `RequirementResult.shortTitle` — a short name used only where a row is REFERENCED ("24 regular-course credits", "9 credits at Notre Dame", "Core: Operating Systems", "Specialization (3 groups)", "Research seminar (2 cr)", "Transfer credit (§5.2)"). The requirement's own card keeps the full title, and the link carries it as a tooltip. The e2e fails if any name in the list exceeds 32 characters. | **DGS** | — |
| 2026-09-08 | A course's "Counts toward" cell named only the credit pool it fed, though one course routinely serves several requirements — a 60000-level course feeds the total credits, the regular-course credits, the nine at Notre Dame, a §4.4.1 core area and a §4.4.2 specialization group. | The cell now lists every requirement the course feeds, each linking to its card, split into what it counts toward NOW and what it WILL count toward once passed or approved. `audit()` builds the index by reading the rows' own `satisfiedBy` (already written for the processing request) and a new `pendingBy` the other way round, so the list can never disagree with the report; ids that are not courses (the residency row lists semesters) are skipped. `CourseLine.counts` carries it. | **DGS** | §4.2, §4.4.1, §4.4.2 |
| 2026-09-08 | A course the sheet marks `any` can satisfy any §4.4.2 specialization group, and the dropdown offered a bare list — the student could not tell which choice would actually help. | The engine works out, per flexible course, which groups the student's OTHER courses do not already cover (`groupChoices` on the §4.4.2 row, read off the same matching the requirement uses, so a suggestion is never a group another course is filling). The dropdown sorts its options under "Groups you still need" and "Already covered by another course", and when nothing is chosen a line names the useful groups. Choosing one of them clears the line — no nagging once the student has answered. | **DGS** | §4.4.2 |
| 2026-09-08 | Usability pass over both pages on computer, tablet and phone (DGS request). Six independent reviews produced 51 findings; twelve were verified and fixed. | **First visit**: an untouched record now leads "Getting started — add your coursework to see where you stand" and folds the requirement list into "What this degree requires — N checks" instead of showing thirteen red "Not yet" rows about a student who has entered nothing. The dial ring is navy while a student is simply unfinished, red only when a deadline is overdue. **Phone**: a whole `@media (max-width: 600px)` block was dead (it preceded the base rules it meant to override), so the add-a-course form was two columns at 390 px and cards kept desktop padding; the import buttons and the prior-study radios now meet a 44 px touch target; the status pill parks in one column down the report. **Everywhere**: a disclosure the student opened survives the next edit; adding a course by hand says so; "Load example" marks the record and shows a banner until it is cleared, and warns before switching an M.S. student to §4; the § chip is styled as the control it is and the glossary sits under the summary rather than 9 000 px later; the Grad Admin chip counts what the card lists; the attention list is ordered most-actionable first and no longer truncates mid-word; a course-rules search that matches nothing says so rather than showing an empty table; the rules line no longer reads "updated after X and up-to-date as of X" on the day of a sheet edit; the clipboard fallback no longer tells a phone user to press Ctrl. Fourteen further findings are listed in docs/STATE.md for the DGS to weigh. | **DGS** (request), Claude (which twelve) | — |
| 2026-09-08 | Georgia Tech's UNOFFICIAL transcript prints "Georgia Inst. of Technology"; the abbreviation reached the student's coursework headings, the DGS review request and the Grad Admin processing request. | Institution names are spelled out wherever anyone reads them: `expandInstitutionAbbreviations` (data/external.ts) expands Inst./Univ./Coll./Poly./Intl./Natl./Engr. with or without the full stop, and Tech./Sci./Agri. only with it. The parsed name is stored expanded, so every surface follows; hand-typed names expand too. `normalizeUniversity` applies the same expansion, so a sheet row written either way still matches. Deliberately conservative: "Georgia Tech" and "Virginia Tech" are names in their own right and are untouched, and "St." is left alone since it is Saint in one name and State in another. | **DGS** | — |
| 2026-09-08 | Georgia Tech prints "ID" for Industrial Design, and the parser threw those courses away because "ID" was on the list of codes that read as an identifier. | "ID" is off the list. A line is refused as an identifier only when it has the shape of one: the subject "ID" followed by a number and NO title. A student id is too long to match the course-number pattern anyway. `src/transcript/external.ts`. | **DGS** (bug report) | — |
| 2026-09-08 | Georgia Tech's header prints "Georgia Inst. of Technology", which the institution test did not recognise, so the university was left blank. | The strong-name pattern accepts the abbreviation: `inst(itute)?. of tech`. | **DGS** (bug report) | — |
| 2026-09-08 | A USC Master's that took three years was split as if it were a 4+1, its first year marked undergraduate. | Spanning more than two years is not evidence of an undergraduate degree — a Master's can simply take longer. The two-year "Taken as" estimate now runs only when the transcript NAMES a bachelor's degree (dated or not, `bachelorsNamed`). With no bachelor's on the record, every row keeps the slot's level and the student corrects any row by hand: the app does not guess. `src/transcript/level-prefill.ts`. | **DGS** (bug report) | §5.2 |
| 2026-09-08 | `nd_credits` holds one fixed number, which cannot describe a course worth 2 credits one term and 4 the next, and every quarter-system course had to be entered by hand. | New ExternalCourses column `credit_system` (`quarter` / `semester` / blank). Set on any one row of a university, it applies to every course from that university, listed or not: the credits printed on the student's own transcript are converted (× 2/3), so a varying course converts correctly every time. `nd_credits` keeps its meaning as a FIXED per-course value and still wins where it is set. The converted value is kept EXACTLY (DGS: nine quarter hours are exactly six, not a rounded 7.5 that would cost half a credit against the §5.2 cap); only the printing is shortened to two decimals (`formatCredits`). The course line says "converted from the quarter system" so a student can tell a conversion from the DGS's own figure. | **DGS** (both design choices) | §5.2 |
| 2026-09-08 | A previous-transcript preview cut the "Taken as" dropdown in half and scrolled sideways (DGS screenshot): three rows whose YEAR the parser could not read. | A row got the one-line layout whenever the import was text-layer, even when the parser had missed the credits, the grade or the year — those cells then hold full-size controls, and the one-line form is told not to wrap, so the row overflowed the card. A row is now compact only when the transcript gave every value (`rowIsCompact` in `src/transcript/preview-layout.ts`); anything still asking the student for a value keeps the labelled layout, which wraps and labels its own "Taken as". The nowrap rule is also scoped to rows without inputs, as a second guard. | Claude (bug), **DGS** (reported) | — |
| 2026-09-08 | Uploading a Master's transcript reset the "Bachelor's degree awarded" term the student had entered after uploading their undergraduate transcript. | A term the student set by hand now wins: the preview is pre-filled from it, not from the new transcript's conferral date, and says so ("Taken from 'Bachelor's degree awarded' under Your standing — importing this transcript does not change it", naming the transcript's own date so they can correct it deliberately). A term only INFERRED from an earlier transcript still gives way to a later transcript's conferral line, as before. `bachelorsPrefill` in `src/transcript/preview-layout.ts`. The add toast now announces the term only when it actually changed, and calls it "from the conferral date on your transcript" only when it was. | **DGS** | §5.2 |
| 2026-09-07 | The self-check took today's date from the device, so a student abroad, or one with a wrong clock, was audited against the wrong day and could see the wrong semester and deadlines. The DGS asked for "the internet clock for Notre Dame, IN's time zone", shown on the loading page. | Two parts, neither of which contacts a third party. The zone comes from `Intl` with `America/Indiana/Indianapolis` (St. Joseph County, Eastern with US daylight saving), so the calendar is always read as it reads in South Bend. The instant comes from the HTTP `Date` header of one same-origin `HEAD` request to the page's own URL — CLAUDE.md allows same-origin loads of the app's own assets and forbids runtime calls to anyone else, and a public time API would have made the page's printed promise ("nothing is transmitted to the University or to any third party") untrue. `no-store` forces a real request and `Age` is added back if a cache answered; a 2 s timeout, and any failure falls back to the device clock with the loading card saying so. `src/data/clock.ts`; the loading card gains a "Checking today's date at Notre Dame" step; both pages take the resolved date. The two privacy paragraphs now name this second request. | **DGS** (request), Claude (same-origin rather than a public time API) | — |
| 2026-09-07 | The bare "Fall 2026" chip beside "2. Your standing" did not say what it was, and could be read as the entry term. | It reads "current semester: Fall 2026" — it is today's semester, nothing the student entered. | **DGS** | — |
| 2026-09-07 | "Entered the program" named neither the program nor the institution. | The legend names both, from the selected program: "Entered the Ph.D. program at Notre Dame CSE in" / "Entered the M.S. in CSE program at Notre Dame in". Messages that quoted the old field name now say "your entry term". | **DGS** | §3, §4 |
| 2026-09-07 | "Bachelor's degree awarded" was optional. | It is required, whether or not the student holds a prior graduate degree: §5.2 counts a course as transfer credit only when it was taken after the bachelor's degree. The legend reads "(required)", the field carries `required`/`aria-required` while unset, its hint is always amber when unset, and `audit()` adds a warning once any coursework is entered. Nothing is blocked — the page has no submit — but the field can no longer be read as an extra. | **DGS** | §5.2 |
| 2026-09-07 | "Courses needing DGS or advisor sign-off" showed "Needs DGS review" even when every course was pre-approved (only Grad Admin processing left) or when only the advisor had to act. | The row is now "Courses still to be approved or processed" and splits its courses by who must act: the DGS decides, the advisor approves, the Grad Admin processes. Its status is "Needs DGS review" ONLY when the DGS still has a course to decide; otherwise "In progress"; "Does not apply" when nothing is outstanding. A non-CSE course needs both the advisor and the DGS, so it appears in both lists. `signOffActors` in shared.ts holds the routing; advisor-summary.ts routes the same strings and must stay in step. | **DGS** | §3.2/§4.2/§5.2 |
| 2026-09-07 | Prior Notre Dame rows were levelled only when they were first filed, so importing the transcript and then setting "Bachelor's degree awarded" (the natural order, since the field sits below the transcripts card) left them at the level guessed from the course number, while the opposite order used the award term. | `reclassifyNotreDameCourses` now re-levels rows that are already filed as prior on every call, so the two actions commute. The transcript's own UG/GR label still wins, and clearing the term falls back to the course number. Credit was never affected (the engine reads `bachelorsAwarded` directly), but the course line and the coursework heading were. A hand-entered Notre Dame row dated before entry also follows the award term rather than the level typed in the form. | Claude (bug found by the shipping-verification pass), **DGS** approved the fix | §5.2 |
| 2026-09-07 | "Undergraduate credits do not transfer (§5.2)" reads as if it were about the COURSE's level, so a student who took a 500/600-level course before their bachelor's expects it to transfer. | Reworded everywhere it appears (coursework group heading, previous-transcript preview, the "nothing could be used" message): "Courses taken as an undergraduate student do not transfer, whether or not the course itself is a graduate course (§5.2)." | **DGS** | §5.2 |
| 2026-09-07 | Undergraduate course lines said only what they do for §4.4.1 core knowledge, never why they earn no credit (the 2026-09-06 shortening left that to the group heading). | Each undergraduate line now carries a second term, "taken as an undergraduate student — no transfer credit (§5.2)", after the core-knowledge term — reversing half of the 2026-09-06 decision at the DGS's request. Order matters: the core-knowledge term stays first so the line keeps its colour (green when a core area is earned, amber when one is possible, red otherwise). | **DGS** | §5.2, §4.4.1 |
| 2026-09-07 | A pre-approved transfer's coursework line read "pending DGS review — would count toward regular courses (3 cr) once approved; transfer credit (§5.2); pre-approved in the DGS's external-course rules — …" — the two halves contradicted each other. | Ruled transferable in the ExternalCourses tab means nothing is left for the DGS to decide: the line now leads "pre-approved by the DGS — will count toward regular courses (3 cr) as transfer credit once the Grad Admin has processed it; send the Grad Admin the processing request (§5.2)" (still amber until processed), and the transfer card is "In progress" instead of "Needs DGS review" whenever every pending transfer is pre-approved, its detail "Pre-approved by the DGS: … — final once the Grad Admin has processed the transfer". Unreviewed or undecided courses keep "pending DGS review" and the card keeps "Needs DGS review". `allocate.ts` buildExplanation, `phd.ts` transferRow. | **DGS** (request), Claude (wording, the "In progress" status) | §5.2 |
| 2026-09-07 | The compact preview (a 560–860 px card) showed no column headers; the DGS asked for one "Taken as" header above the dropdown column and no others. | The header row is shown again in text-layer previews with only the "Taken as" cell visible, centred over the dropdown (fixed at 118 px); the other headers stay clipped for screen readers, as the whole row was before; OCR and hand-typed rows keep their per-field labels. e2e pins the single visible header and its alignment at 1400 and 1100 px. | **DGS** | — |
| 2026-09-07 | Shorter "Taken as" labels; semesters shown in tables | "UG student" / "Grad student" (DGS: shorter than "Undergraduate student" / "Graduate student"; the manual form's two choices start the same way). Semesters in TABLE CELLS use the short form FA26 / SP25 / SU25 (`termShort` in engine/term.ts: season code + two-digit year) — the previous-transcript preview, the Notre Dame import preview and the coursework table — rendered as an `<abbr>` whose tooltip is the full name. Prose, notes, toasts, checkbox labels, aria-labels and the emails keep "Fall 2026" (`termLabel`); widen the scope on request. | **DGS** (request); Claude (scope: cells only) | — |
| 2026-09-06 | Which transcript wording supplies the bachelor's award date that pre-fills "Bachelor's degree awarded"? | Any dated conferral OR completion line (DGS, late evening): on the degree line itself ("— Conferred: May 11, 2024"), on a labelled line within the next six ("Degree Date", "Degree Completion Date", "Conferral Date", "Date Conferred", "Graduation Date", "Awarded", "Completed on"), or on such a line up to two lines BEFORE the degree name; "05/2024" counts as that month. A forecast ("Expected graduation …") never counts, and the scan stops at the next degree's name, so a master's date is never taken for the bachelor's. The Notre Dame parser accepts the same labels. `src/transcript/external.ts`, `parse.ts`. | **DGS** (request), Claude (patterns) | §5.2 |
| 2026-09-06 | University names in the review request were upper-cased ("PURDUE UNIVERSITY", the sheet's own style), and the engine's "ruled this … course non-transferable" line used the sheet's spelling. | Keep the name as the transcript prints it / as the student's record has it — no upper-casing in the request's headings or paste-ready ExternalCourses rows, and the engine line uses the course's own institution (the rules match names case-insensitively). Hand-typed names keep the Title Case decided earlier today. | **DGS** | §5.2 |
| 2026-09-06 | "Taken as" showed "Undergraduate" next to graduate-level (500/600) courses taken before the bachelor's; students read it as the course's level. | The value is the STUDENT's status when they took the course: the options read "Undergraduate student" / "Graduate student" (preview and manual form, whose label is now "Taken as"), and the column tooltip and the notes say so — "a graduate-level course taken before your bachelor's degree was awarded still counts as undergraduate coursework: §4.4.1 core knowledge only, no transfer credit (§5.2)". | **DGS** (request), Claude (wording) | §5.2, §4.4.1 |
| 2026-09-06 | Should the Grad Admin processing request be active only when there is transfer credit, a milestone or the MSCSE to process? | No — active as soon as ANY requirement is met (DGS, late evening): met requirements are worth recording even with nothing to transfer. They count as items on the card, which says "N requirements met so far — the request lists each with the courses, semesters or dates that meet it, for the record". | **DGS** | §4, §5.2 |
| 2026-09-06 | Copy dialogs: should the on-screen notice say explicitly that the message shown is what was copied? | Yes. An emphasised lead line sits right above the message: "✓ The following message has been copied to your clipboard. Read it through — it is exactly what you will paste into the email." When the browser blocked the clipboard it reads "The following message was NOT copied — your browser blocked the clipboard. Select it and copy it yourself (Ctrl/Cmd+A, then Ctrl/Cmd+C)." (`src/ui/copy-dialog.ts`). | **DGS** (request), Claude (wording) | — |
| 2026-09-06 | Second pass on the evening batch (DGS, after review) — 1: the bachelor's award term for a combined transcript | In the Previous Master's Transcript preview a combined record — rows of both levels, a bachelor's conferral line, or the two-year rule firing — shows a "Bachelor's degree awarded" control that is REQUIRED (Add refuses until it is set), pre-filled from the transcript's conferral date when there is one ("Read from your transcript … check it"); a term the student sets re-fills every dated row's "Taken as" by it (in or before → Undergraduate) and is written to the record as the student's own choice; a term read from the transcript fills the record while the student has not set one by hand. Other rows keep the plain pre-fill. | DGS (mandatory + pre-fill); mechanics default (Claude) | §5.2 |
| 2026-09-06 | Second pass — 2: the award term versus a `transferable = yes` ruling (supersedes the answer recorded earlier that evening) | The award term is absolute: a course dated in or before it earns no transfer credit even when the ExternalCourses tab rules it transferable — the ruling is about the course, §5.2 criterion 2 about the student. Its DGS-confirmed core area still shows on the line (green). | DGS | §5.2 |
| 2026-09-06 | Second pass — 3, 4, 5: the processing request for the Grad Admin | (3) The same markers as the review request: the student's own words end at "(You may edit anything above this line)", a divider and "(DO NOT MODIFY ANYTHING BELOW THIS LINE)" sit right above the first table. (4) Below the line, besides the transfer tables, EVERY requirement the self-check shows as met appears as a table of what satisfies it — the courses the engine counted (course, title, credits, grade, term, where), the full-time semesters for residency, the date for a milestone, the figure for the GPA; the engine records this per row (`satisfiedBy`) so the tables come from data, not prose. (5) Both requests stress that the ORIGINAL transcripts must be attached; the Grad Admin button also saves the self-check file (the "Save to a file" export) and the dialog lists numbered steps: paste (or select-and-copy when the clipboard is blocked), attach the original transcripts, attach the saved file (Grad Admin only), send. | DGS (request); table shapes and step wording default (Claude) | §5.2 |
| 2026-09-06 | Two rows in the ExternalCourses tab for the same university + course (a decided row pasted below an older blank one) | The last row wins — it replaces the earlier one, and the diagnostics warn ("row N replaces row M; delete the older one"). Was: the first row won, so a later correction was ignored. The Courses tab's own duplicate rule (same course_id + effective_term → first row wins) is unchanged — say if it should follow. | DGS | — |
| 2026-09-06 | The "Ask the DGS to review" card vanished after a Remove + Undo or when the ExternalCourses tab changed (DGS report) | Two causes, both fixed. (1) A row in the ExternalCourses tab counted as "decided" even with both verdict cells blank — pasted straight from the request — so the course left the card for good, and a blank `satisfies_core_area` was never asked about. Now a row is a decision only where its cells say something: `transferable` (yes/no) for §5.2, `satisfies_core_area` for §4.4.1; a core-sounding title (Algorithms, Operating Systems, Architecture) keeps asking until the core cell is filled — and `none` is now an allowed value there ("decided: no core area"; DGS-approved schema change, `data/README.md`), blank still means undecided. The rule is one pure function, `coursesNeedingDgsReview` (src/engine/review.ts), with a test matrix. An unlisted non-CSE Notre Dame course (MATH 60610) now also gets a paste-ready Courses-tab row. (2) The Undo button lived inside the page, which every change rebuilds, so a keystroke, a checkbox or the "Reading the transcript…" toast destroyed it. Toasts now live in a stack outside the page: a plain message (4 s) never replaces an Undo; each Undo is its own element (12 s for a row, 20 s for a transcript-wide removal), cancelled by Load example / Clear / Load a file; Undo puts every row back at its original index and returns focus to the Remove button. | DGS (report; `none` approved); mechanics default (Claude) | §4.4.1, §5.2 |
| 2026-09-06 | Copying a request or summary showed a four-second toast — no time to check the message, the name or the address | Every copy button (review request → DGS, summary → advisor, processing request → Grad Admin) now opens a dialog that stays until OK: who it is for (role, name and address from the contact card; the advisor by the name entered under Milestones), the subject line, the message itself in a read-only box, and how to attach transcripts; when the browser blocks the clipboard the same dialog says so and the message is selected for a manual copy. Escape or OK closes it and focus returns to the button. | DGS (request); the dialog's contents default (Claude) | — |
| 2026-09-06 | The DGS's and the Grad Admin's roles were blurred on the page ("DGS office", "ask the Grad Admin") | Two people, two jobs: the DGS determines eligibility by the handbook and the course rules — the review request asks for that and goes to the DGS alone; the Graduate Program Administrator (Grad Admin) processes what has been decided and keeps the official record (§5.2: "A student should send the credit transfer request to the Graduate Program Coordinator and the DGS will approve…"). New card "Ask the Grad Admin to process" (after the milestones, always shown, with the two-roles statement) and a copy-ready processing request with the DGS in cc carrying everything processable (DGS answer): transfer credits ruled transferable in the ExternalCourses tab (table: university, course, title, credits, ND credits, grade, term) or already approved (the student's §5.2 attestation), the milestone dates entered, the qualifier completion form once the components are done, the MSCSE along the way once met, and the requirements the self-check shows as met. The advisor summary gained a fourth list, "What the Grad Admin needs to do"; a pre-approved transfer is the Grad Admin's to process, not the DGS's to decide; the student's list says which request to send whom. Rewordings: the dial sublines, the glossary's DGS entry plus a Grad Admin entry, the print header, the qualifier-form milestone label ("filed with the Grad Admin (DGS office, §4.4)"), the attestation hint, the footer, the "pre-approved" course lines and the MSCSE-along-the-way line, the contact card's Grad Admin scope. Not changed (the DGS worded them 2026-09-01, proposals only): the accuracy notices saying "the rules the DGS and the Grad Admin use". | DGS (request and answer); wording default (Claude) | §5.2, §4.4, §4.5 |
| 2026-09-06 | Title of the §4.4 umbrella card | "Qualifying examination — all components" (five cards sit under it: three core areas, specialization, research); the first detail part now says "core knowledge (§4.4.1 — one card per core area below)". The handbook's "three components" stays in the quote, the glossary and the advisor summary. | DGS | §4.4 |
| 2026-09-06 | Manual course form: universities from the external-course rules, capitalisation, Level choices | The University box (manual form and the previous-transcript preview when editable) offers the distinct universities of the ExternalCourses tab and Title-Cases whatever is typed on leaving the box ("purdue university" → "Purdue University"; "UCLA" kept; of/at/the… lower-case) — Title Case for every hand-typed name (DGS answer); matching the rules ignores case anyway. Level is two choices — Graduate, taken after the bachelor's degree (§5.2 transfer candidate; saved with no degree level, so the row belongs to no transcript slot) and Undergraduate, taken before it (core knowledge only) — the Master's/Ph.D. distinction belongs to the transcript rows and "Prior graduate study". | DGS (request and answers); defaults (Claude) | §4.4.1, §5.2 |
| 2026-09-06 | Only courses taken with graduate student status can transfer; graduate-level courses taken before the bachelor's degree do not count (DGS) — implemented in the engine | §5.2 criterion 2 ("the student had graduate student status when they took these courses") by the student's own record: a new optional "Bachelor's degree awarded" term under Your standing (filled in by a transcript import that finds a dated bachelor's award and flagged until the student touches it — a later import replaces an inferred value, never a chosen one; Remove leaves it alone). In the engine a transfer course dated in or before that term earns no credit whatever its number or registration level — "not counted — taken before / in the term your bachelor's degree was awarded (…), so not as a graduate student (§5.2)" — while its §4.4.1 core note stays (confirmed → green, keyword → amber, else red; lines excluded for the window or the grade floor with a confirmed core area turn green too). A DGS ruling transferable = yes in the ExternalCourses tab still wins (DGS answer), and the line then names the date so the request can mention it. Unknown award term → unchanged (each course's level decides; the standing card warns when graduate prior coursework exists without it); an award term not before the entry term → a report warning. Prior Notre Dame rows without a UG/GR label follow the award term before the course number. Tests: scenarios phd-transfer-before-bachelors, phd-nd-grad-before-bachelors; e2e: the ND fixture's B.S. is dated 2021 and the run flips the term to 2024 and back. | DGS (rule and answer); mechanics default (Claude) | §5.2 |
| 2026-09-06 | "Oral Candidacy Exam (OCE)" in full at every mention read heavy | The full name appears once per surface — the first mention in reading order on the page (the standing-card note on a fresh record, else the milestones field; the report then says "OCE — §4.5", "OCE passed", "Passing the OCE…") and once per copied email — and "OCE" afterwards. The glossary keeps the full term (it is the definition). The engine keeps writing the full name; each surface shortens the repeats. | DGS (request); glossary exception (Claude) | §4.5 |
| 2026-09-06 | Name of the §4.5 examination on the page | "Oral Candidacy Exam (OCE)" wherever the wording is the tool's own: the report's group heading and row title ("Oral Candidacy Exam (OCE) passed"), the milestones field, the glossary entry (now: the candidacy examination of §4.5, sometimes called the dissertation proposal — a written proposal and an oral exam before the committee — to be taken before the end of the eighth semester), the standing-card note on deadlines, the along-the-way MSCSE row, and the advisor summary's to-dos ("Take the Oral Candidacy Exam (OCE) by …", "Decide how to handle the passed Oral Candidacy Exam (OCE) deadline", "Confirm the late Oral Candidacy Exam (OCE)"). The handbook sentences quoted behind the § chips stay verbatim ("The candidacy exam must be taken before the end of the eighth semester…"); the requirement id `phd.candidacy` and the sheet's parameter keys are unchanged. | DGS (the name); where it appears: default (Claude) | §4.5 |
| 2026-09-06 | Should the opening notice remember the acknowledgment (30 days per browser)? | No — it keeps asking on every visit. | DGS | — |
| 2026-09-06 | The request about undergraduate rows that was cut off after "If it is relevant," | Ignored for now — the behaviour of the same-day row on undergraduate lines stands (a line says which core area the course satisfies or may satisfy, or that it is not relevant). | DGS | §4.4.1 |
| 2026-09-06 | The two-year "Taken as" rule when the transcript itself tells the level of some courses | When the transcript says which courses were taken as a graduate (or undergraduate) student — a UG/GR column, a "Level" block, a dated bachelor's conferral — those labels pre-fill "Taken as" and the two-year rule is not applied at all; rows the transcript leaves unlabelled keep the row's default (Graduate in the Previous Master's row). The rule now runs only for a record that says nothing about levels. Test: tests/level-prefill.test.ts. | DGS | §5.2 |
| 2026-09-06 | The wording review (W1–W47 in docs/WORDING-REVIEW.md) | Approved as drafted, all of it; the file is removed. From now on a student-facing string Claude drafts is listed, numbered, in the reply that delivers it. | DGS | — |
| 2026-09-06 | In an 1100 px window the previous-transcript preview showed two-line rows, although the same-day decision (below) says one line from a 560 px preview | The stylesheet had keyed the one-line rows on a 600 px preview; an 1100 px window leaves the preview 582 px (a 1400 px window 642 px). The code now follows the recorded 560 px, and the e2e run checks it in Chrome AND on Safari's engine (new that evening: `E2E_BROWSER=webkit npm run e2e`, Playwright's WebKit) at 1400 and 1100 px — every cell on the row's first line, the credits / grade / term / level columns at the same x in every row, nothing overflowing; a long title still wraps inside its own cell (two of the five fixture rows do at 1100 px). | code aligned to the recorded decision (Claude); the look at 1100 px confirmed by the DGS the same evening | — |
| 2026-09-06 | Should the copied review request say which half the student may edit? | Yes: "(You may edit anything above this line)" sits directly above the divider, mirroring the existing "(DO NOT MODIFY ANYTHING BELOW THIS LINE)" below it — in both the plain-text and the HTML flavour (bold, like the lower marker). Also fixed on the way: "Prior graduate study: Completed prior M.S. or Ph.D.." no longer ends with two full stops. | DGS (wording) | — |
| 2026-09-06 | While a transcript preview is open, what happens to the other Import and Remove buttons? | They are inactive — greyed, but still reachable: hovering shows why, and a click repeats it as a toast instead of acting ("Not available while a transcript preview is open: finish selecting and adding those courses (“Add …”), or cancel the preview, and this button becomes active again."). Applies to every button on the four transcript rows — the Notre Dame Import / Import again / Remove and each previous-transcript slot's Import and Remove — whichever preview is open (the Import buttons were already blocked, silently, since 2026-09-03; the Remove buttons were not). Implemented with `aria-disabled` rather than the `disabled` attribute, because a disabled button gets no hover, no focus and no click, so it could never explain itself (screen readers announce it as unavailable either way). | DGS (request); the wording default (Claude) | — |
| 2026-09-06 | Which imported fields may a student edit in the previous-transcript preview? (second pass — supersedes the same-day row below) | For a system-generated (text-layer) PDF: none of the values read from it — the university name, and each row's course number, title, credits, grade and term are shown as printed and cannot be edited; only "Taken as" stays a control (the level is a judgment the transcript may not state). A value the parser could not read (a grade, credits or a year) stays an input, or the row could never be completed — the hint says so, and rows without a grade are still not added. OCR previews and rows added by hand stay fully editable. The DGS's earlier reason for keeping credits/grade/term editable (the parser can misread numbers) is answered the other way: a misread on a text-layer PDF is a parser bug to report, not something for the student to patch. | DGS | — |
| 2026-09-06 | The preview rows were too tall ("too thick, making the list too long"), and the "not selectable — hover for why" tag was not wanted | A text-layer row is ONE line when the preview is at least 560 px wide (every desktop layout of the page): "☐ **CS 25100**  Data Structures and Algorithms   4 cr  A  Fall 2023  [Undergraduate ▾]" — no field labels, the columns aligned across rows by fixed minimum widths (plain flexbox: a first version used CSS subgrid, which Safari mis-rendered — overlapping cells, the row overflowing the card — so it was replaced the same afternoon), a long title wrapping inside its own cell, and the level dropdown explaining itself on hover ("Taken as — the level you were registered at …") since it has no label. Narrower (phones): two lines, the values on the second with "·" between them and a small TAKEN AS label. Rows went from about 120 px to about 45 px. The visible "not selectable — hover for why" tag is gone (DGS: not necessary) — the greyed row and disabled box are the cue, the reason stays on hover and in the box's aria-describedby. Editable rows (OCR, typed by hand) keep the labelled inputs, since a blank input without a label is unusable; the header row still names every column for screen readers. | DGS (request: one line, no labels, no tag); layout default (Claude) | — |
| 2026-09-06 | The Notre Dame transcript import could not be taken back like a previous-university transcript | The Notre Dame row now names what the import added ("7 courses from your transcript") with a Remove button and an "Import again" button (the import stays available: the transcript grows every semester; the hint recommends Remove, then import the updated PDF). Remove takes back exactly the rows the import added — program courses, pre-entry prior coursework and the transcript's own transfer-credit block, all flagged `fromNdTranscript` on import — plus the GPA it filled in (a hand-typed GPA has no source and stays) and a "Prior graduate study" it inferred when no graduate prior transcript remains; rows typed by hand stay, and so does the entry term (the student can change it under Your standing). Undo instead of a confirm dialog, as for the other transcripts. Rows saved before the flag existed are removed one by one in the table, as before. Test: tests/prior-nd.test.ts; e2e step 9. | DGS (request); what Remove clears: default (Claude) | — |
| 2026-09-06 | Unreviewed graduate courses from another university read as if they counted ("pending DGS review — would count toward regular courses once approved"), with the allocator deciding which of them fell over the cap | Every unreviewed graduate course from a prior program is a CANDIDATE for transfer credit until the DGS rules (DGS: "only the courses related to CSE will count, and that will be decided by DGS; until then, all courses are candidates"). Each line reads "pending DGS review — candidate for transfer credit (§5.2); would count toward regular courses (3 cr) if the DGS approves it" (amber), or, when the candidates together exceed the cap, "counts only if the DGS picks it — the candidates together exceed the 6-credit transfer cap" (still amber — the lines never rank the candidates); a DGS-confirmed core area is appended as before. The general rule is said once above each transcript's group in the coursework card: "Transfer credit (§5.2) is decided by the DGS course by course — only CSE-related courses transfer, at most N credits in total, and the Graduate School confirms the DGS's recommendation. Until the DGS has ruled, every graduate course here is a candidate; the review request below asks for those rulings." — shown only while the group holds a candidate (a group whose only course the handbook rules out — grade floor, five-year window — keeps its red line alone). Courses the DGS has ruled on (transferable yes/no) and attested transfers keep their former lines; the report's running totals are unchanged (the allocator still counts candidates under the cap provisionally). Test: tests/scenarios/phd-transfer-cap-binds.json. | DGS (rule); wording default (Claude) | §5.2 |
| 2026-09-06 | Undergraduate rows in a previous-transcript preview that cannot matter: selectable or not? | Not selectable (DGS: "Don't let users select it"): the box is disabled, the row is greyed (a visible "not selectable — hover for why" tag was added and removed the same day — DGS: not necessary), and the row explains itself on hover and to screen readers: "Not selectable: this course is not related to the core-knowledge areas (Algorithms, Operating Systems, Computer Architecture — §4.4.1), and undergraduate credits do not transfer (§5.2), so there is nothing to add. If you took it as a graduate student, change “Taken as” to Graduate and it becomes selectable." Relevance = a core-keyword title, a DGS ruling in the ExternalCourses tab, or (Notre Dame) a Courses-tab core area — the same test the coursework card uses. Changing "Taken as" re-renders the row: to Graduate it becomes a §5.2 candidate and is ticked; back to Undergraduate it is unticked and locked again; Select all skips locked rows. | DGS (request, including the Graduate escape hatch) | §4.4.1, §5.2 |
| 2026-09-06 | The editable previous-transcript preview needed sideways scrolling ("not all the inputs/options are shown at once") | The eight-column table needs about 860 px, which no layout of this page gives it (the two-column desktop layout leaves the card about 670 px; the one-column layout below 900 px at most about 810 px). Below that width — keyed on the preview's own width (a CSS container query), not the viewport — each course becomes a small bordered form: tick, course id and title on the first line, credits / grade / term / year / "Taken as" with small labels on the second, every field visible, nothing to scroll sideways; a host page that embeds the app in a wider column gets the table back. | DGS (request); layout default (Claude) | — |
| 2026-09-06 | The rules took long to load ("probably because too many tabs are being downloaded — any chance this can be merged into one download?"), and after a first fix (one tab at a time) the load failed outright ("each download takes too much time, so it always failed in 15 seconds") | Measured from the DGS's Mac over the day: any one published-CSV request answers in 0.3–1 s, but about one request in fifteen simply stalls for 10 s or more — whether the tabs are fetched together, two at a time or one after another (fetching three or four at once stalls more often). A single 15-second request per tab therefore failed whenever ANY of the four requests stalled, and one stall used up the whole budget; fetching one at a time did not change that. The fix is not to wait on a stalled request. A later measurement the same day (144 requests, 12 rounds each of four-at-once, two-at-a-time and one-after-another, interleaved) showed the stall rate is the same at any concurrency — 8, 9 and 8 stalls of 48 — with no 429s or other rate-limiting, so (the DGS's question "can we download all tabs at once?") all four tabs are fetched at once, and each attempt is HEDGED: when a request has not answered after 2 s, a second request for the same tab is sent alongside it and the first answer wins (the other is dropped); a request that fails at once (dropped connection, 5xx) is hedged immediately. A whole attempt is abandoned after 6 s (then 10 s, then 14 s for the second and third attempts — the later ones longer so a slow but working connection still gets through) and the tab asked for afresh, up to three attempts; a 4xx or an unpublished sheet is never retried. A stall therefore costs about two seconds, not a failed page. The loading card says when a tab is being asked for again ("Google is slow; asking again" for a hedge; "still nothing; asking again (attempt 2 of 3)" for a fresh attempt) and promises "usually a few seconds; a slow answer is asked for again, up to about 30 s in all". One download is not available: the endpoint serves one tab per request (the whole-document HTML export is a script shell without the tables, the xlsx export answers 400 — the DGS found the same trying to publish the workbook as one Excel file — and the gviz endpoint is blocked by CORS), and the app must not proxy through any server (FERPA promise; no backend). The sync script was already sequential and keeps its 60 s limit. Tests: tests/load-retry.test.ts (fetch stubbed). | DGS (report; all-at-once after the measurement); hedge/retry design default (Claude) | — |
| 2026-09-06 | External transcripts: courses whose subject code is longer than six letters were skipped (DGS bug report: MUSIC, CICS, MATH read; COMPSCI, STATISTC ignored) | Subject codes may run 2–10 letters (was 2–6 in the one-cell pattern, 2–7 in the two-column pattern). Capitals decide (DGS suggestion — "most course IDs are capital letters"): a subject of seven letters or more counts only when it is printed in capitals ("COMPSCI 501", "STATISTC 501", "ENGLWRIT 112", "BIOCHEM 285"), so prose such as "Chapter 3" or "Building 12" is not a course; subjects of up to six letters stay case-insensitive ("cs 5321", decision of 2026-09-04). The stopword list of words that look like codes when followed by a number grew (course, section, chapter, level, class, student, record, graduate, undergraduate, academic, degree, program, plan, college, school, campus, catalog, major, minor, date, printed, issued, standing, status, version, grade(s), credit, hour, unit, point, subtotal, average, number, code, title, passed). Checked against 17 sanitized transcripts: 16 unchanged, one gained its three 8-letter-subject courses. Test: tests/external-transcript.test.ts. | DGS (bug report and the capitals heuristic) | §5.2 |
| 2026-09-06 | Advisor summary, second pass: list the requirements by section, colour the components, and end with who does what | Supersedes the same-day "busy advisors" row (its subject line, standing paragraph, deadline footnote, alpha notice and the re-voicing of engine details stay). The email now lists every requirement IN HANDBOOK ORDER, one section per report group (Basic requirements §2.2–2.3 → Coursework §4.2 → Residence and time §4.3 → Qualifying examination §4.4 → Candidacy §4.5 → Dissertation and defense §4.6–4.7; §3.x for the M.S.), each row tagged with the page's status word and coloured — MET green, IN PROGRESS and NEEDS DGS REVIEW amber, NOT YET and CANNOT EVALUATE red (the page palette #10693f / #8e5108 / #a81e14, inline in the HTML flavour; bracketed tags in the text flavour) — followed by its why and its semester deadline (a passed deadline red). "Does not apply" rows and the per-course sign-off list are left out of the sections. Then three lists — "What I need to do", "What I need from you, my advisor", "What the DGS needs to do" — derived from the rows: remaining credits from the threshold rows ("Complete 46 more credits toward the total-credit requirement (9 of them in progress) (§4.2)"), remaining full-time semesters, the seminar still to take, qualifier components with their deadlines (a passed one adds "ask the DGS about an extension" for the student and "decide whether to extend" for the DGS), the specialization course below the grade floor, the candidacy exam by its deadline, dissertation items only once candidacy is passed, the M.S. project report (student completes, advisor approves) and thesis defense; the pending-approval courses give the student "Send the DGS the review request for …", the advisor "Approve <course> — <reason>" when the reason names the advisor, the DGS "Decide on <course> — <reason>" when it names the DGS or a review; an unrecorded plan-of-study approval is the advisor's; a missing rules-sheet parameter is the DGS's; a list with nothing says "Nothing at the moment." Wording is Claude's draft for the DGS to adjust. Tests: tests/advisor-summary.test.ts (actionItems rules included). | DGS (request); wording default (Claude) pending DGS edits | — |
| 2026-09-06 | Combined BS+MS transcript without level markers: how is "Taken as" pre-filled? | By term (DGS rule): the courses of the LAST TWO YEARS of the record — the final four fall/spring semesters, summers between them included, ending with the latest term on the transcript — are marked Graduate, earlier ones Undergraduate. Applied only in the Previous Master's Transcript row (where a combined transcript goes; a Ph.D. record spans more than two years by nature and an undergraduate transcript is undergraduate throughout) and only when the rows span more than two years; rows the transcript itself labels (a UG/GR cell, a Level block, a dated bachelor's conferral) keep their label, rows without a year keep the slot's level. Known limit, accepted: a part-time M.S. taken over more than two years gets its early courses pre-filled Undergraduate — the preview says how the column was filled and asks for a check. The preview explains the source of every value ("How “Taken as” was filled in: …" — the transcript's markers / the two-year rule with the window named / the row it was imported in) and asks the student to double-check before adding; the added-courses toast repeats the split. Tests: tests/level-prefill.test.ts. | DGS (rule); Master's-row-only and part-time caveat: default (Claude), pending DGS confirmation | §5.2 |
| 2026-09-06 | Which imported fields may a student edit in the previous-transcript preview? | The university name is shown read-only when the transcript supplied it (the DGS's rules key on the name as printed); the course number and title of every row read from a system-generated (text-layer) PDF are shown as text, not inputs. Credits, grade, term, year and "Taken as" stay editable — the parser can misread numbers and grades, and the level is a judgment. OCR previews stay fully editable (the reader misreads names and codes), as do rows the student adds by hand. | DGS | — |
| 2026-09-06 | Per-course lines were misleading: a course pending DGS review read as if it counted; undergraduate lines were long | Every line now leads with its standing and carries a coloured mark — green ✓ "counts toward …" for credit (or a §4.4.1 core area) earned now; amber ● "pending DGS review — would count toward … once approved" for credit counted only provisionally (transfer, non-CSE, dgs_approval, unknown courses) and "in progress — will count toward … when passed" for IP grades; red ✕ "not counted — …" for credit that earns nothing (over a cap, failed, ineligible). A fully excluded row no longer repeats the approval note (an over-the-cap transfer course used to read "not counted … needs DGS + Graduate School approval"). Undergraduate rows (§5.2 gives them no credit) say only what matters for §4.4.1: "satisfies the <area> core-knowledge requirement (§4.4.1) — confirmed by the DGS" (green), "may satisfy the <area> core-knowledge requirement (§4.4.1) — pending DGS review" (amber), or "not relevant to the core knowledge requirement (§4.4.1)" (red); a DGS-confirmed core area is also said on graduate transfer lines. The mark is a shape as well as a colour (WCAG 1.4.1) with a spoken word for screen readers. The Transcripts card's "What the DGS's rules say" block was removed: everything it showed (confirmed core areas, transferable / not transferable rulings, not yet reviewed) is on the course's own line and in the report's §4.4.1 / §5.2 rows. | DGS (request; redundancy check confirmed by Claude) | §4.4.1, §5.2 |
| 2026-09-06 | Who receives the "Ask the DGS to review" request, and how are the courses listed? | The DGS alone — the card, the greeting ("Dear DGS,") and the toasts no longer mention the Grad Admin (the same-day "Grad Admin" row's greeting is superseded). The course details in the copied request are tables (one per transcript: Course · Title · Credits · Grade · Term · Why it needs a decision) in the HTML flavour, pipe-separated rows under the same header in the text flavour; the paste-ready sheet rows are unchanged. | DGS | — |
| 2026-09-06 | The copied advisor summary carried too much information — what do busy advisors need? | Redesigned for the three questions an advisor asks — which requirements are not met, why, and by when — with everything else short or gone (DGS request; wording drafted by Claude for the DGS to adjust). Subject line: "Degree self-check — Ph.D., entered Fall 2026 — 6 requirements not yet met, 1 deadline passed" (or "nothing not yet met — N in progress" / "all checked requirements met"). One standing paragraph: "Here is my current standing from the CSE degree self-check tool, as of March 1, 2028." / "Ph.D. (Handbook §4); entered Fall 2026; no prior graduate degree; cumulative GPA 3.50." / "7 of 18 requirements met · 4 in progress · 6 not yet met · 1 needs DGS review." Then NOT YET MET — what is missing, and by when: a numbered list, deadline passed first, then nearest deadline, then handbook order; each line "**name** (§) — why. Due by the end of Spring 2028." / "Deadline passed (was due during Spring 2028)." (names red bold in HTML per 2026-09-04; a passed deadline red bold too). Then NEEDS DGS REVIEW and CANNOT EVALUATE (headings match the page's status words), IN PROGRESS (first statement of each detail, deadline where there is one), and "Met:" as names on one line. Removed: does-not-apply rows; the course-by-course list of 2026-09-03 (the printed report has it); the separate DEADLINES block of 2026-09-05 (each line carries its own deadline); "(approximate)" on every line (said once in the footnote "Deadlines are counted from Fall 2026 and given by semester; they are approximate — the registrar's calendar sets the exact dates."); the RULES_ACCURACY / PDF-import / coverage caveats (they address the student — the banner, footer and opening notice keep them). The closing notice is the DGS's BETA_NOTICE plus the handbook edition: "Alpha version under testing. Informational only, no warranty — not an official degree audit; every final decision rests with the Director of Graduate Studies. Checked against the CSE Graduate Studies Handbook, July 2026 (URL)." The engine's details are written for the student at the page, so the email re-voices them: sentences about the page (checkboxes, attestation, the course rules page) are dropped, "Talk to the DGS" goes, an "Overdue —" sentence gives way to the deadline phrase, "Confirm your advisor approved your plan of study … tick the attestation" becomes "Advisor approval of my plan of study (§3.2/§4.2) is not yet recorded", "Enter your cumulative GPA…" becomes "Cumulative GPA not entered yet", and you/your read I/my. Example Ph.D. record: 766 → about 430 words. Tests: tests/advisor-summary.test.ts. | DGS (request); wording default (Claude) pending DGS edits | — |
| 2026-09-06 | How is the Graduate Program Administrator referred to on the pages? | "Grad Admin" is the abbreviation. The contact card introduces it — "Graduate Program Administrator (Grad Admin)" — and every other mention on both pages, in the copied review request ("Dear DGS and Grad Admin," — changed to "Dear DGS," later the same day, see the review-request row) and advisor summary, the engine's detail strings (transfer requests, the MSCSE-along-the-way row) and the toasts says "Grad Admin". The former "Graduate Program Coordinator" in three engine strings was the same person and is gone. The glossary keeps the full title once. | DGS | — |
| 2026-09-05 | Usability review Phases 1 and 2 — wording and structure (DGS asked for all remaining items; wording drafted by Claude for the DGS to adjust) | Self-check page: (8) the alpha and privacy banners became ONE slim strip — "Alpha — under testing. Informational only, no warranty — every final decision rests with the Director of Graduate Studies (DGS). The course rules are accurate; only this tool's application of them is still being tested. Feedback: <DGS email>." and "Private by design. Everything you enter — and any transcript PDF — stays in your own browser; nothing is uploaded. The page's only network request is the read-only fetch of the public course rules." — with a "Details" expander holding the DGS's full paragraphs unchanged (they also stay in the footer and the copied summary); (9) the opening notice still appears on every visit (decision of 2026-09-03 kept), its button now reads "I understand — continue"; (10) instructional sentences shortened to ≤25 words with the same facts (Transcripts intro, unofficial-transcript note, combined-transcript callout, Coursework intro, footer self-check paragraph — which now names the Graduate Program Administrator, matching the contact card, instead of "Graduate Program Coordinator"); (11) a permanent hint under the entry term ("Every deadline and the residency count are counted from this term") and a Milestones hint that every date is optional; (12) "Prior graduate study" and "Project or thesis option" are radio buttons; (13) "Select all / Select none" above both transcript previews, the Add buttons count the ticked rows, the external preview's title column takes the free width; (14) import buttons read "Import from PDF (alpha)" — "alpha" kept per 2026-09-04; (15) deadlines in body-size type with a "Deadline:" lead word, amber when due soon, red when passed; (16) status words in sentence case: Met / In progress / Not yet / Needs DGS review / Cannot evaluate / Does not apply (was "N/A"); (17) the headline counts "N of M met · N in progress · N not yet (N need a DGS decision)"; when everything passes: "All automatically checkable items are currently satisfied — final confirmation by the DGS is still required"; meters past their target read "12 (9 needed) ✓"; (19) the rules-sheet diagnostics expander shows only when the sheet has errors; (20) printouts start with "Self-check printed on <date> — <degree>, entered <term> — not an official audit"; (21) footer: "Deadlines are shown by semester and are approximate; the registrar's calendar sets the exact dates"; (27) a "Needs your attention (N)" list at the top of the report — not-met, needs-review and cannot-evaluate rows, each linking to its card with the card's first sentence — the handbook order of the cards is kept; (28) a "Terms used here" glossary at the end of the report (cumulative GPA, regular course, full-time, residency, qualifier, core knowledge, specialization, research qualifier, candidacy, transfer credit, DGS), each entry citing its §; (29) unmet core-knowledge, specialization and regular-course rows link to the matching course list; (31) the input cards are numbered 1–4 (Transcripts, Your standing, Coursework, Milestones). Course-rules page: (29) the filters live in the URL (q, program, core, category, type, retired, confirmed, sort, desc, view) so links can be shared; (30) a "What are you checking?" view (Everything / M.S. / Ph.D. / Ph.D. qualifier) hides the columns that do not answer the question and pre-sets the Program filter. (32) docs/USER-TESTING.md is the protocol for trying the tool with three or four students. | DGS (requested all items); wording default (Claude) pending DGS edits | — |
| 2026-09-05 | Phone and tablet versions of both pages | Same pages, responsive (no separate mobile site): at ≤900 px the self-check page shows the score dial and credit meters FIRST, then the inputs, then the full report, with a sticky bar (score + Inputs/Report jumps); at ≤600 px course tables become stacked rows and transcript previews become one small form per course; tablets in landscape keep two columns. The course-rules page at ≤860 px shows one card per course with a Sort control instead of sortable headers. Verified at 390 px and 820 px in the e2e run. | DGS (requested); layout default | — |
| 2026-09-05 | Which cumulative GPA does §2.2 read from a combined Notre Dame transcript (undergraduate + graduate, or an earlier graduate program at Notre Dame)? | The transcript's GRADUATE-level cumulative GPA — never the undergraduate figure (a student's combined transcript imported 3.68, the undergraduate GPA, because Banner's "All Levels" totals list Graduate before Undergraduate). When graduate courses from an earlier program at Notre Dame precede the entry term, the registrar's graduate figure includes them; the preview then also shows the GPA of this program's courses alone and lets the student choose, with the transcript's figure as the DEFAULT (the handbook's "cumulative GPA" is the registrar's number; the Graduate School computes it over all graduate coursework at Notre Dame). The DGS may overrule for a given student — flagged for confirmation. | default (DGS-delegated), pending DGS confirmation | §2.2 |
| 2026-09-05 | Usability review Phase 0 — accessibility mechanics (no wording changes) | Applied at the DGS's request: keyboard focus survives the page rebuild after every change; the opening notice is a native modal dialog (Escape dismisses it like Agree; nothing is stored either way); visible labels replace placeholders in the course form; fieldsets for the two-part questions; program tabs expose their pressed state; the Remove buttons are named and undoable (Undo in the toast) instead of confirm dialogs; import failures and the add-course validation persist inline instead of vanishing toasts; landmarks and skip links on both pages; the gold text colour darkened to #7a6220 for contrast (rules and borders keep the light gold); 24 px minimum targets; wide tables scroll inside their card on phones; course-rules page: labelled filters, Clear filters, live result count, spacing-insensitive search, sortable headers with `aria-sort`, a caption, the DGS notes behind a per-row Notes button (hover tooltip retired), the legend before the table; an automated accessibility gate (axe-core) and phone-width check in the e2e run. Phases 1–2 (wording, notices, report ordering, course-page views) wait for the DGS's wording approvals. | DGS (approved the phase); details default | — |
| 2026-09-05 | Should the opening (consent) notice carry the coverage caveat? | No — removed from the opening notice at the DGS's request ("Not all cases are covered yet — for example, transcripts whose layout the parser has not seen, or a combined BS/MS record whose undergraduate and graduate courses are not told apart on it."). The notice now has three parts: not yet approved by the department / testing and informational only; feedback welcome with the DGS's address; Agree. The same caveat still ends the alpha banner, the footer and the copied advisor summary (`BETA_SCOPE_NOTICE`), pending a separate DGS call. | DGS | — |
| 2026-09-05 | Where does a combined BS+MS transcript go, and which transcripts read best? | (1) The Transcripts card now carries a highlighted callout above the three previous-degree rows: one transcript covering both a BS and an MS (a 4+1 / 5+1 program, or both degrees at the same university) is imported ONCE, in the Previous Master's Transcript row — each course's level is read from it and shown in the "Taken as" column for correction; the Undergraduate row works too; never import the same PDF twice. The mixed-level preview note repeats "import it just once". (2) A "Prefer unofficial transcripts" note in the card's intro: the web / self-service PDF is recognized best (one column, no watermark); official transcripts (two columns, security patterns, e-transcript covers) are read too but their previews deserve a closer look. | DGS (2026-09-05 requests 1 and 4) | — |
| 2026-09-05 | Deadlines are shown as semesters, not dates | Supersedes the date-based wording of the same morning. Every deadline chip, detail and advisor-copy line names a SEMESTER; the ISO date stays inside the engine for ordering and comparisons. Mapping (src/engine/term.ts `deadlineTerm`): a term's nominal first day → "before Fall 2034" / "the start of Fall 2034" (the eight-year and five-year limits, which fall on the entry term's anniversary); a term's nominal last day → "by the end of Spring 2030" (the fourth-semester qualifier and eighth-semester candidacy deadlines); any other day → "during Spring 2028" / chip "mid-Spring 2028" (the 18-month research qualifier). Chips: "Due before Fall 2034 — 8 years after entry (approximate)", "Due by mid-Spring 2028 — 18 months after entry (approximate)", "Due by the end of Spring 2030 — semester 8 (approximate)"; overdue chips say "the deadline was the end of …" / "the 8-year limit passed at the start of …". The advisor copy's DEADLINES block reads "counted from Fall 2026; semesters are approximate — the registrar's calendar sets the exact dates". Tests: tests/engine-units.test.ts (chip wording), tests/advisor-summary.test.ts. | DGS (2026-09-05 request 3) | §3.3, §4.3, §4.4, §4.4.3, §4.5 |
| 2026-09-05 | Parser accuracy from the DGS's 17 de-identified text transcripts and 3 scans (USC, Buffalo, Northeastern ×3, Duke, IIT, UC San Diego, a Notre Dame official Parchment PDF, a Notre Dame Banner 9 web transcript, Johns Hopkins, a two-column undergraduate record with COPY tiles, UW, SWJTU-Leeds, Georgia Tech, ShanghaiTech, Western Ontario; Ghana, Accra Technical, a US undergraduate scan) | Shared reader (src/transcript/layout.ts, used by BOTH imports): (1) pages are read in their reading orientation — a /Rotate 90 landscape page (Northeastern) and a page whose content is drawn sideways (the official Notre Dame PDF after re-saving) were previously dropped whole as "rotated watermark" (0 courses); (2) a security band drawn as one run repeating a phrase ("DUKE UNIVERSITY ? DUKE UNIVERSITY ? …", UC San Diego's) is dropped, and the phrase it repeats becomes a one-line institution header when it names one; (3) short-word tiles ("COPY" at ≥ 4 x positions, ≥ 8 times) are dropped; (4) two-column detection takes the column's edge as the leftmost x where a substantial share of its runs start (not the leftmost run, not the busiest x) and requires the band before it to be empty. External parser (src/transcript/external.ts): codes spanning two cells with the title in the number's cell (Western "COMPSCI   3331A Title"), dotted numbers with a leading division cell (Johns Hopkins "EN  CSCI  601.226"), two-part subjects ("E E", "A A" at UW), three-letter suffixes (Buffalo "106LEC"), a stray 1–3-letter security mark before the code, credits with three decimals ("3.000"), a bare integer before a decimal credit staying in the title ("Calculus 1   4.000"), a grade before the credits (USC "CSCI-549   B   4.0   Title"), Georgia Tech's one-letter H-column flag dropped, titles after the numbers or on the adjacent plain line (ShanghaiTech; bilingual Chinese records), a numeric token echoing the credits ("3.00   3.00   12.00") never read as a grade, totals lines ("OVERALL 54.47") never read as courses, PeopleSoft's "Term  Course  Transfer Course" table skipped like Banner's transfer block, "Academic Year 2013-2014" as a year header, and A+ mapped to A (the top of Notre Dame's scale — the student can change it). Levels: a cell that is exactly Undergraduate/Graduate, "Student Level : Graduate", a term header ending in the level ("Fall Quarter 2029 Graduate"), "Beginning of UNDERGRADUATE Record", and a closing "GRADUATE SEMESTER TOTALS" line. Degrees: a "Degrees Awarded" block or a "Degree and Date Conferred" table header makes the degree lines under it conferred; the bachelor's date may sit up to four lines below ("Confer Date: …"). University guess: a name spaced across the page ("UNIVERSITY   OF   SOUTHERN   CALIFORNIA"), registrar/provost signature lines and sentences rejected, "Office of the Registrar" / "Course Numbering" / "Grade Scale" tails stripped, "College of …" anywhere treated as a division. Notre Dame parser (src/transcript/parse.ts): Banner 9's Campus column before the Level ("CSE 60641 Main GR Title"), the "Transcript Level" table, the official PDF's "UNIVERSITY OF NOTRE DAME CREDIT:" section and per-record "Course Level:" lines (the record level beats the course number), the transfer block's institution on the term line ("Fall 2020   College Board"), a Roman-numeral title on a credit-only row ("Calculus I   3.000") not read as an Incomplete, A+ → A. Sanitizers: outputs are named sanitized-<seed>.pdf / sanitized-scan-<tag>.* — never after the input, whose name is usually the student's; page rotation is preserved; Banner level/campus codes are kept. Results on the samples: 11/13 transcripts went from 0–20 course rows to complete or near-complete reads; the scans read 11, 28 and 34 rows with OCR-flagged fields. No sanitized file is committed; the layouts are pinned by invented fixtures in tests/layout.test.ts, tests/external-transcript.test.ts and tests/transcript.test.ts. | Claude Code (DGS request 2 of 2026-09-05) | — |
| 2026-09-05 | Rules spreadsheet renamed | The Google Sheet is now **CSE-Degree-Checking-Rules** (was CSE-Degree-Audit-Rules; spelling confirmed by the DGS — plural "Rules"). `SHEET_NAME` in src/ui/sheet-source.ts, the e2e check, the test, and every doc mention (README, MAINTENANCE, CLAUDE.md, data/README.md, data/sheet-urls.json's comment) carry the new name, with "named CSE-Degree-Audit-Rules until 2026-09-05" where a reader might know the old one. The published-CSV links and the edit URL are unaffected by a rename; the historical START-HERE.md and this log keep the old name. The DGS renames the sheet itself in Drive. | DGS | — |
| 2026-09-05 | Combined transcripts (4+1 / 5+1 BS-MS, a BS and an MS at one institution) and Notre Dame as a previous institution — the corner cases the coverage notice named | (1) Every previous-transcript row has a "Taken as" level (Undergraduate / Graduate): read from the transcript when it says — a UG/GR cell on the row, a "Level: Graduate" / "Term Totals (Undergraduate)" / "College: Graduate School" block, or a DATED bachelor's conferral line, which splits the rows by term (up to that term undergraduate, after it graduate — a 4+1's senior-year graduate courses start as undergraduate, since §5.2 needs graduate student status, and the student can flip them) — else the slot's level. On add, undergraduate rows are Bachelor's coursework whatever the slot (§4.4.1 only, no transfer credit) and graduate rows take the slot's degree (Master's; Ph.D. in the Ph.D. slot; a graduate row on an undergraduate transcript is Master's coursework); any graduate row sets "Prior graduate study" as a Master's transcript does (completed on a graduate-degree conferral line, else not completed + warning). On a mixed-level transcript the undergraduate rows that cannot matter (no core-keyword title, no ruling, no Courses-tab core area) stay visible but unticked instead of being left out, so a level can still be corrected; a single-level undergraduate transcript keeps the 2026-09-04 filter. (2) The previous-degree slots ACCEPT a Notre Dame transcript as the record of an earlier Notre Dame degree: read by the Notre Dame parser (UG/GR column, degrees awarded), filed under "University of Notre Dame", its transfer-credit block left out with the usual note, with a reminder that a transcript that also holds the current program belongs in the Notre Dame row (which separates the earlier degree from the program by the entry term). The OCR path still redirects a scanned Notre Dame transcript to the digital one. (3) The review request asks about prior Notre Dame coursework as Notre Dame courses: a Courses-tab row when the course is not listed there ("does it cover a §4.4.1 core area?"), otherwise a §5.2 recommendation; the on-page verdict line shows the Courses tab's core area for a listed course ("course rules"). (4) COVERAGE_NOTICE's examples change accordingly: "transcripts whose layout the parser has not seen, or a combined BS/MS record whose undergraduate and graduate courses are not told apart on it" — the DGS may re-word. Tests: tests/external-transcript.test.ts (levels), e2e steps 7–8 (combined-transcript.pdf fixture; the ND fixture in the Ph.D. slot). | Claude Code (defaults; DGS asked for these corner cases 2026-09-05) | §4.4.1, §5.2 |
| 2026-09-05 | Deadlines and residence were counted from the wrong term (student bug report: a combined Notre Dame transcript with eight undergraduate semesters showed 13 consecutive semesters, an 8-year limit of 2034-08-15, an 18-month research-qualifier date and a semester-8 candidacy date all off) | Root cause: the app never learned the entry term — "Entered the program" silently defaulted to the fall of the current year, and residence counted every Notre Dame term on the transcript. Fixes: (1) The Notre Dame import READS the entry term. Precedence: a stated admit/matriculation term (the latest, when several curricula state one) → the latest graduate-level term whose Banner block says "Student Type: New…" → the first graduate-level term (rows registered at the graduate level: the transcript's UG/GR column, else the term's level block, else the course number — 6xxxx and up graduate, below 5xxxx undergraduate) → with no graduate-level rows, the first term after the last degree awarded, else the earliest term. The preview offers it as a ticked checkbox ("Set 'Entered the program' to Fall 2024 — the first graduate-level term on your transcript"); the standing card then flags the term as read from the transcript and says that the residency count and every deadline (§4.3 eight-year limit, §4.4.3 eighteen months, §4.4 four semesters, §4.5 eighth semester) are counted from it; touching the dropdown clears the flag. A fresh record's default term is flagged "assumed" the same way. (2) When a Notre Dame degree is awarded with graduate-level terms on both sides of it (a prior Notre Dame M.S. versus an MSCSE earned along the way; a 4+1), the EARLIER reading is kept — earlier deadlines are the safe mistake — and the other reading is spelled out on the preview and the standing card ("if that was a separate degree finished before this program, the entry term is Fall 2026"). (3) Residence (§4.3 / §3.3) counts only terms from the entry term on, in the engine and in the full-time checkboxes. (4) The advisor summary (text and HTML) carries the deadlines: a DEADLINES block right after the standing line ("counted from Fall 2024; dates are approximate — the registrar sets the calendar"), every not-done deadline as a date in date order, plus "Due by <date>" / "overdue — was due by <date>" on each such requirement line (a Deadline column in the HTML tables). Test: tests/transcript.test.ts (combined transcript), tests/scenarios/phd-nd-undergrad-before-entry.json, tests/advisor-summary.test.ts, tests/engine-units.test.ts (residency guard), e2e step 2. | Claude Code (DGS bug report; DGS: "read the start time from the current Notre Dame PhD transcript and show the deadlines … include the deadlines in the text copies that goes to the advisor") | §4.3, §4.4, §4.4.3, §4.5, §3.3 |
| 2026-09-05 | How Notre Dame coursework taken BEFORE the entry term is treated (a combined transcript: Notre Dame B.S., 4+1, or prior Notre Dame M.S. before the Ph.D.) | Filed as PRIOR COURSEWORK, not program coursework: origin 'transfer', institution "University of Notre Dame", degreeLevel from the level the student was registered at (the transcript's UG/GR column, kept per course as registeredLevel; the course number as a fallback — a 6xxxx course taken as an undergraduate is undergraduate coursework). Re-filed automatically whenever the entry term changes, so correcting the dropdown needs no re-import. Consequences: no residence (§4.3), no §4.2 credits, no §4.4.2 specialization from prior coursework. Prior UNDERGRADUATE courses: never transfer (§5.2) but satisfy §4.4.1 core knowledge ("either at Notre Dame or at their previous institution") — for a Notre Dame course the Courses tab's core_area decides, whenever the course was taken, so no ExternalCourses ruling is needed (course line: "satisfies the Operating Systems core-knowledge requirement (§4.4.1) — a Notre Dame course listed in the course rules; no transfer credit (undergraduate, §5.2)"); an unlisted one whose title matches the core keywords is pending review as before. Prior GRADUATE courses (a prior Notre Dame M.S.): §5.2 transfers like any other prior M.S. course — "These five requirements also apply to the transfer of credits earned in another program at Notre Dame" — capped by the prior-study caps, DGS + Graduate School approval, pre-approvable in ExternalCourses under UNIVERSITY "UNIVERSITY OF NOTRE DAME"; their §4.4.1 core area still comes from the Courses tab. The import sets "Prior graduate study" from them the way a Master's transcript does (completed when the transcript shows a graduate degree awarded, else not completed + the warning). The §4.2 "nine credits at Notre Dame" count stays program coursework only ("regardless of any credits transferred"). The coursework card lists them under "Notre Dame, before entering the program — undergraduate/graduate coursework" (undergraduate rows filtered to core relevance, as for other universities); the Transcripts card's Bachelor's/Master's slot shows them and its Remove button drops them. In the ND preview, pre-entry undergraduate rows that cannot matter (no core-keyword title, no Courses-tab core area, no ruling) start unticked. | Claude Code (default; DGS told of both defaults 2026-09-05) | §4.4.1, §5.2, §4.2, §4.3 |
| 2026-09-05 | Sharing sample transcripts for parser work | Real transcripts are never shared. The DGS may share a DE-IDENTIFIED copy made on his computer by the repo’s sanitizers (`npm run sanitize -- file.pdf` for text PDFs; `python3 scripts/sanitize-scan.py` for scans), which keep the layout the parsers react to (positions, widths, rotations, run boundaries, structural vocabulary, subject codes, credit values, institution names and watermarks) and remove the record: names/addresses/titles → random letters of the same shape (titles kept only with --keep-titles), ids → random digits, years shifted by one document-wide offset, dates’ days changed, e-mail local parts changed (domains kept), letter grades replaced, GPA/points/totals randomized, course numbers keep their first digit only. Both tools print everything kept verbatim and the scan tool cannot alter text OCR fails to read, so the DGS reviews every output before sharing; outputs are git-ignored. | DGS | — |
| 2026-09-05 | Transcripts with a text watermark (UMass Amherst, Western Ontario: the university’s name repeated across the background) read few or no courses | Text watermarks are removed before any line is formed, by position alone (no wording assumed): rotated runs are never body text, and a phrase repeated six or more times on a page at three or more x positions is a tiled background — dropped only where it repeats down the page, so the one genuine header line naming the university survives. A phrase that merely repeats down one column (a thesis-credit title every term) is kept. Because the DGS cannot share those transcripts (FERPA), `npm run diagnose -- file.pdf` prints only their SHAPE (counts and letters→a/A, digits→9 lines) for parser work; the regression fixture is the invented Banner transcript under synthetic tiles and a diagonal banner. | DGS | — |
| 2026-09-05 | Prior-university transcripts in the Banner two-column layout; what counts as a Notre Dame marker | Triggered by the DGS’s own IIT transcript, which the Ph.D. slot refused as "looks like a Notre Dame transcript" and would otherwise have read as one course. (1) An nd.edu e-mail address (a current ND student’s contact address on file elsewhere) and a "Notre Dame, IN" mailing address are NOT Notre Dame markers — only "University of Notre Dame", the running-totals label, or an nd.edu/insideND URL are; both parsers share the test. (2) Banner-style official transcripts are read column by column (two text columns per page, detected from the text positions; a one-column table is never split), and a SUBJ / NO. pair printed in separate columns is one course code. (3) Rows under "TRANSFER CREDIT ACCEPTED BY THE INSTITUTION" are that university’s record of a THIRD school’s courses — they are left out of the import with a note in the preview (import that school’s own transcript instead), so undergraduate work cannot masquerade as graduate transfer credit. (4) Zero-credit rows (seminars, internships) keep credits 0; a title keeps its "&"; each imported row starts from the season of its own term header instead of always Fall; the institution is taken from the legend page when the header names only colleges and programs. The alpha caveat about the PDF import stays; every row is still previewed for correction. The real transcript was used only to diagnose and was not kept; the regression fixture is invented. | DGS | §4.4.1, §5.2 |
| 2026-09-05 | Self-check page: say that not all cases are covered yet, and welcome all feedback | The opening "Before you continue" notice and the alpha disclaimer (banner, footer, and the copied advisor summary, which share BETA_SCOPE_NOTICE) now state: "Not all cases are covered yet — for example, 5+1 BS/MS programs, or a BS and an MS earned at the same institution." (COVERAGE_NOTICE in src/ui/handbook.ts — keep the examples in step with the engine). The feedback line reads "Error reports, suggestions, and feedback are all welcome — please email the DGS (…)" in the banner and footer, and "Any error report, suggestion, or feedback is welcome — please contact the DGS (Prof. <name>, <email>)" in the opening notice. The course rules page is unchanged. | DGS | — |
| 2026-09-05 | Course rules page: say that core knowledge can be met by prior undergraduate or graduate coursework | A paragraph under "Core knowledge areas §4.4.1" states that the requirement can be met by the listed Notre Dame courses or by prior coursework at a previous institution — undergraduate or graduate — quoting §4.4.1 ("either at Notre Dame or at their previous institution"), that such a course counts once the DGS has confirmed it, and that the self-check tool prepares the review request from imported transcripts; the legend’s "Core knowledge" bullet says the same in one sentence. Presentation only — the rule (2026-09-01: no level limit; DGS ruling via ExternalCourses) is unchanged. | DGS | §4.4.1 |
| 2026-09-04 | Footer note about the rules spreadsheet shortened | Supersedes the footer wording in the "Link the rules spreadsheet" decision below: readers, even faculty, do not need the sheet’s internals (tabs, published-copy mechanics, error-report line). Both footers now say only that the data come from the DGS’s rules spreadsheet (linked), which is created based on the CSE Graduate Studies Handbook (linked), that the spreadsheet is accessible by faculty only, and that students see the same rules on the course rules page (the course rules page says it shows the same rules). The masthead "Source:" line is unchanged. | DGS | — |
| 2026-09-04 | Advisor summary: unmet requirements stand out by name | In the "Copy summary for your advisor" email, every requirement whose status is Not yet (unmet) has its NAME highlighted: red bold in the HTML flavor (inline style, the page’s --bad red — email clients drop stylesheets) and wrapped in double asterisks in the plain-text flavor, which cannot carry color. Only the unmet status qualifies — needs-review, cannot-evaluate and in-progress rows are not "not met" and keep their plain names; the attention-first grouping is unchanged. Wording/presentation only — no rule logic changed. | DGS | — |
| 2026-09-04 | Link the rules spreadsheet from both pages, and say who can open it | Both the self-check tool and the course rules page link the CSE-Degree-Audit-Rules Google Spreadsheet — the DGS’s rules sheet — twice: a one-line "Source:" note under the dated line in the masthead, and a footer paragraph explaining what it is (four tabs: Courses — which courses count toward each degree and their core-knowledge/specialization tags; Parameters — every number the handbook states plus the contacts; Categories — the §4.4.2 specialization categories; ExternalCourses — rulings on courses from other universities), that the page reads its published copy on every load, and that the spreadsheet itself is accessible by faculty only — students do not need it because the course rules page shows the same rules and the self-check applies them; faculty who spot an error email the DGS. The link is the sheet’s ordinary edit/view address from `data/sheet-urls.json` (`sheet_edit_url`), never a published-CSV address; the sheet stays shared with faculty, not students. Wording/presentation only — no rule logic changed. | DGS | — |
| 2026-09-04 | Core rows say "pending review"; course lines name the §4.4.1 upside; qualifier list lives on the course rules page | (1) A §4.4.1 core-knowledge row that could be satisfied by a course awaiting DGS review — an unreviewed prior-institution course (any level) or an unlisted ND course whose title matches the area's keyword — now shows needs-review with "Pending review: <course> — its title suggests <area>…" instead of "No <area> course yet"; a course the DGS has ruled on (even with no core area) is decided, never pending. Precedence: met > in progress > pending review > not yet. (2) A prior GRADUATE course's coursework line now says when it may count beyond §5.2: unreviewed keyword-titled courses append "may still satisfy the <area> core-knowledge requirement (§4.4.1) after DGS review" (transfer-ineligible ones) or "the same review can confirm…" (transfer-eligible ones). (3) The approved specialization list is NO LONGER announced by email each semester — the §4.4.2 row and the legal footer now point to the course rules page instead. | DGS | §4.4.1, §4.4.2, §5.2 |
| 2026-09-04 | Graduate transcripts: core-keyword courses join the review request even when transfer-ineligible | An unreviewed course from a prior GRADUATE transcript whose title matches the core keywords (algorithm/operating/architect) now joins the pending-DGS-review list and the copied review request even when the engine has ruled out its §5.2 transfer credit (outside the five-year window, below the B floor) — it may still satisfy the §4.4.1 core-knowledge requirement, which has no such restrictions. Its reason line reads "no transfer credit, but the title suggests a §4.4.1 core area — not yet reviewed by the DGS". Courses the DGS has already ruled on are unaffected. | DGS | §4.4.1, §5.2 |
| 2026-09-04 | PDF-parsing caveat in the disclaimer; contacts move to the sheet; nested detail bullets | (1) The alpha disclaimer (banner, footer, copied summary) adds: the transcript-PDF import is under development and still highly inaccurate — check every imported course against the actual transcript. (2) The names/emails of the DGS, ADGS, and Graduate Program Administrator come from six optional Parameters rows (contact_dgs_name/email, contact_adgs_name/email, contact_grad_admin_name/email) so a DGS handoff is a sheet edit, not a code change; a missing or blank row keeps the fallback baked into src/ui/contacts.ts (refresh those occasionally). Shown in the contact card, the opening notice, and the review-request emails on both pages. (3) A detail part may carry a lead sentence plus enumerated items, rendered as a two-layer bullet list (outer bullet = the lead, one sub-bullet per item) — applied to the sign-off list and the §4.4.2 qualifying-course list; the copied summary keeps the flattened prose. | DGS | — |
| 2026-09-04 | Alpha label; undergraduate courses filtered to core relevance; along-the-way MSCSE needs 6 ND research credits; transfer card ignores undergrads; parser reads more layouts; "completed" counts as conferral; long details bulleted | (1) The banner and import buttons say ALPHA again. (2) Undergraduate imports and the coursework list show only courses that can matter — a title matching the core keywords (algorithm/operating/architect) or a course the DGS has ruled on — with a note that undergraduate credits do not transfer (§5.2) and only Algorithms / Operating Systems / Computer Architecture-relevant courses are listed; the per-course "counts toward" line for an undergraduate course now says only which §4.4.1 core area it satisfies (DGS-confirmed) or may satisfy after DGS review. (3) The along-the-way MSCSE (§4.5) requires, at Notre Dame: candidacy passed + ms_regular_credits_min regular credits + ms_project_credits_min research credits, where research = courses the sheet types research or project (research/dissertation, thesis-project direction; independent study excluded — DGS choice). (4) The "Transfer credit from a prior M.S." card neither lists nor counts undergraduate courses. (5) External parser: lowercase course codes accepted (with a stopword guard so FALL/GPA/TOTAL lines cannot become courses), numeric grades (85, 9.5) kept as rawGrade for the student to map, and two-line rows (code+title, then numbers) merged — fixes transcripts that read only a handful of courses. (6) A "Degree Completed: Master of Science" line counts as conferral; "Not completed"/"Incomplete" never does. (7) Requirement cards render a long multi-statement detail as a bulleted list (detailParts); the copied summary keeps prose. | DGS | §4.4.1, §4.5, §5.2 |
| 2026-09-04 | Course mappings attributed to the Graduate Studies Committee and the DGS; official-rules statement moved to the courses-page masthead | The claim that mappings are "set by the Director of Graduate Studies with faculty input" was wrong: they are set by the Graduate Studies Committee and the DGS (committee name as in the handbook). The "Official course rules" sentences now open the masthead itself — replacing the old descriptive intro while keeping the handbook link, the degree self-check link, and the rules-updated/up-to-date date line — and the notice banner below keeps the remaining notes (Pending rows, not-currently-offered, retired courses, handbook-wins, corrections email). | DGS | — |
| 2026-09-04 | Courses page: core knowledge and specialization presented as Ph.D. Qualifying Examination requirements | The overview's two grids now sit under one "Ph.D. Qualifying Examination courses (§4.4)" heading with an explainer that core knowledge (§4.4.1) and specialization (§4.4.2) are the exam's two course-based requirements, apply to Ph.D. students only (MSCSE students can ignore them), and are separate from degree credit. The table's columns are retitled "Core knowledge" and "Specialization" with sub-labels "Ph.D. qualifying exam §4.4.1/§4.4.2" (MSCSE/Ph.D. columns gain "degree credit" sub-labels for the contrast); the filter reads "Any specialization"; the legend and masthead say the same. Wording/presentation only — no rule logic changed. | DGS | §4.4, §4.4.1, §4.4.2 |
| 2026-09-04 | Unfinished-by-default prior study; deduped deadline cards; along-the-way row label; no report scrollbar; courses-page offering note | (1) Supersedes part of the 2026-09-03 conferral decision: a Master's/Ph.D. transcript WITHOUT a conferral line now auto-sets "Prior graduate study" to "Prior M.S., not completed" (still only when it was unset), with a standing-card warning and a toast telling the student to pick "Completed" if they did earn the degree — the transcript proves graduate study happened, just not completion, and "not completed" is the conservative §5.2 cap. The inference is tracked (priorMsInferred): touching the dropdown makes the value the student's own, and removing the last graduate transcript's courses resets it to "none". (2) When a requirement card shows a deadline chip, the in-progress detail no longer repeats the date or the policy (candidacy, qualifier umbrella, research qualifier, both time limits) — the chip carries the when, the § chip carries the rule; overdue rows say "Overdue — talk to the DGS" (research: "and your advisor"). (3) "MSCSE awarded along the way" drops the "(information)" suffix — the row is still unscored. (4) The report column no longer scrolls inside itself (no sticky/max-height) — the page is the only scrollbar. (5) The course rules page states that even an active (non-retired) course may not be offered in a given semester or year; "Typically offered" is a planning hint and the class search is authoritative. | DGS | §4.4, §4.5, §5.2 |
| 2026-09-03 | Approval gate; core-keyword undergrad review; conferral-driven prior study; detail philosophy; along-the-way MSCSE gate; tabled advisor summary | (1) Every visit opens with a dismissable notice: the tool is not yet department-approved, for testing and informational purposes only, errors/feedback to the DGS (Prof. Taeho Jung, tjung@nd.edu); "Agree" dismisses it and nothing is stored. (2) Unreviewed UNDERGRADUATE courses whose titles match the DGS keywords (algorithm / operating / architect) join the review request — no transfer credit, but the DGS may confirm a §4.4.1 core area; other undergraduate courses stay out. (3) A Master's/Ph.D. transcript with a same-line graduate-degree conferral (conferred/awarded/granted) sets "Prior graduate study" to completed when it was still unset — never overriding a student's choice; without positive evidence the value stays and the reconcile warning asks. (4) Detail philosophy: a MET requirement explains how it is satisfied; an unsatisfied one shows just the progress — the rule text lives behind the § chip. (5) Transfer row title shortened to "Transfer credit from a prior M.S.". (6) The along-the-way MSCSE (§4.5) additionally requires ms_regular_credits_min regular course credits completed AT Notre Dame: met only with candidacy passed AND the credits; passed without them shows progress. (7) Save-card buttons shortened to fit one line (Save to a file / Load a file / Copy summary for advisor / Print). (8) The advisor summary's email (HTML) renders each part as a table and lists only courses that count toward requirements. | DGS | §2.3, §4.4.1, §4.5, §5.2 |
| 2026-09-03 | Rules explained on the output side; advisor-ready summary; leaner input cards | Clicking any requirement's § chip in the report reveals the handbook sentence the verdict is checked against (the citation quotes the engine already carried; previously a hover tooltip only). With the rules readable from the report, the input cards drop their policy explanations: the Transcripts intro loses the core/§5.2 sentence, and the Coursework intro loses the regular-course definition and now says imports bring in non-CSE and other-university courses automatically. "Copy summary" is now "Copy summary for your advisor": subject + greeting, one standing line (program, entry term, prior graduate study, GPA), requirements grouped attention-first (Not yet met → Needs a decision or approval → Cannot evaluate → In progress → Met → N/A, with details where they matter), the courses as counted, and the beta/accuracy notices — written to the clipboard in both text and HTML flavors so it pastes cleanly into email. | DGS | — |
| 2026-09-03 | One DO-NOT-MODIFY line; coursework grouped by university and degree; manual undergraduate entries; attach-transcripts reminder | The review request now keeps the human half (greeting, prior study, sign-off) above one divider line followed by "(DO NOT MODIFY ANYTHING BELOW THIS LINE)"; everything machine-readable — the per-tab tables and the per-transcript course details — sits below it, with no per-part markers. The card is titled "Coursework" (was "Coursework at Notre Dame") and the list is grouped by university AND degree: "Notre Dame" first, then one section per (university — transcript) such as "Purdue University — Previous Master's Transcript"; rows are uniform (no University column — the heading carries it). The manual form's "From another university" entries get a degree-level choice — graduate coursework (§5.2), undergraduate (core knowledge only, no transfer credit), previous Master's, previous Ph.D. — so an undergraduate course can be added for §4.4.1 core knowledge even though it never transfers. The request email states "My transcripts … are attached", and the card hint and copy toast remind students to attach the transcript PDFs (Bachelor's/Master's/Ph.D.). | DGS | §4.4.1, §5.2 |
| 2026-09-03 | Transcripts first; split course lists; prior-study reconciliation; request format; one import at a time | The Transcripts card is the FIRST card ("start here") — students are encouraged to begin by importing, and the page fills itself in. The coursework card lists Notre Dame courses in its main table and other-university courses in a separate "From other universities" table naming each university (and transcript slot). If a Previous Master's/Ph.D. transcript is imported while "Prior graduate study" still says none, the standing card shows a warning telling the student which option to pick — the transcript alone cannot prove the degree was completed, so the app never flips the value silently. The review request now opens with the student's prior-graduate-study choice, says "self-check" (never "audit"), marks each paste-ready table "This is the table that can be imported to the DGS's rules sheet — <tab> tab (DO NOT MODIFY THIS PART)", and groups "Course details (DO NOT MODIFY THIS PART)" per transcript (Notre Dame; each slot — UNIVERSITY). Imports are serialized: while any preview is open, every import button is disabled and the card says to confirm or cancel first. | DGS | §2.2, §5.2 |
| 2026-09-03 | One upload home, one review request | Students found separate upload spots and two "Copy review request" buttons (two emails) confusing. All four transcript imports now live in a single "Transcripts" card — the Notre Dame unofficial transcript row (fills the coursework table and GPA) plus the three prior-university rows — and everything still needing a DGS decision (ND courses unknown to the sheet / unapproved dgs_approval / blank verdict, and external courses without a ruling or with transferability undecided) feeds ONE "Ask the DGS to review" card with ONE copy button and ONE email, with a paste-ready tab-separated section per sheet tab (Courses: course_id, title; ExternalCourses: UNIVERSITY, course_id, course_title). Engine-ineligible courses (e.g. outside the §5.2 window) are excluded from the request. | DGS | §3.2/§4.2, §4.4.1, §5.2 |
| 2026-09-03 | GPA placement; mandatory email routing; claim paths retired; review requests for ND courses | (1) The cumulative-GPA field lives in the "Coursework at Notre Dame" card, next to the transcript import that prefills it. (2) Course review requests MUST be emailed to BOTH the DGS and the Graduate Program Administrator — the request greeting and every on-page hint say so. (3) The two pre-ExternalCourses claim paths are retired: the per-area "previously passed elsewhere" checkboxes and the per-course core-area claim on manually added transfer courses — a core area from a previous institution now counts only via a DGS ruling in the ExternalCourses tab (old saved student files still load; the fields are ignored; Q12 superseded). (4) Notre Dame courses that still need a decision — not in the rules sheet (typical for non-CSE), dgs_approval not yet approved, or a blank verdict — get their own copy-ready review request in the coursework card, with paste-ready Courses-tab rows (course_id, title) for the unlisted ones. | DGS | §2.2, §3.2/§4.2, §4.4.1 |
| 2026-09-03 | Simplify university matching; make review requests paste-ready | The `university_aliases` column is retired: courses are matched by the university's name exactly as the transcript prints it, entered in the sheet in capital English (matching still ignores case, punctuation, diacritics and spacing; a leftover aliases column is ignored with one gentle warning). The copy-ready review request now contains tab-separated rows in the ExternalCourses tab's column order (UNIVERSITY, course_id, course_title) so the DGS pastes them straight into the sheet and fills in the ruling columns; the student's credits, grade and term follow as plain lines that stay out of the sheet. The copy carries two clipboard flavors — plain text (tab-separated) and an HTML table — because HTML email flattens tabs to spaces; the table survives Gmail and pastes into Sheets as cells (field-tested by the DGS, same day). | DGS | §4.4.1, §5.2 |
| 2026-09-03 | Review of the four Courses rows flagged as earlier open questions (original wording predates the session records; reviewed from the current sheet data) | CSE 50502 left as-is deliberately: counts_toward_phd stays dgs_approval (each Ph.D. use gets a per-student DGS review) and core_area stays algorithms — a bridge course may confirm the §4.4.1 algorithms area. CSE 60586: category_group cleared — the independent-study project counts toward credit totals but no longer serves as a §4.4.2 category specialization course (the engine applies no course-type filter, so the sheet tag was load-bearing). CSE 60749 / CSE 60774 left as-is: both active, both category sys, no mutual exclusion — a student may count both. CSE 44901: dgs_approval for both degrees, kept inactive so students who took it before Fall 2023 can still enter it; note tidied. All verified against the live published CSV. | DGS | §4.4.1, §4.4.2 |
| 2026-09-03 | Product stage label | The self-check tool is now labelled beta, not alpha: the banner, footer and copied summary open with "Beta version under testing." — substance unchanged (informational only, no warranty; the rules are accurate; only the tool's application of them is still being tested). Internals renamed to match (BETA_NOTICE, BETA_SCOPE_NOTICE, betaNotice(), .banner.beta, .legal-beta). The course-rules page remains unlabelled/official. | DGS | — |
| 2026-09-03 | Naming and notice placement for the importers | The ND uploader button and the external slots' buttons both read "Import Courses from PDF (beta)" — "(beta)" replaces the "untested" note, which is retired. The external card is titled "Prior Coursework" with slots "Previous Undergraduate Transcript" / "Previous Master's Transcript" / "Previous Ph.D. Transcript". A single page-level privacy banner sits directly below the alpha notice: all input, imported PDFs and the optional OCR are processed and stored entirely locally in the user's browser; nothing is uploaded, transmitted, or stored anywhere else; the only network request is the read-only course-rules fetch. | DGS | — |
| 2026-09-02 | Are scanned/photographed external transcripts accepted? | Yes, as an explicit OPT-IN only: a PDF with no text layer offers built-in OCR instead of being rejected. English-language transcripts only (stated in the offer and on the preview); results are approximate, every field must be checked by the student, rows the reader struggled with are flagged ⚠. System-generated PDFs remain the encouraged, exactly-read path, and the ND uploader still accepts no scans. OCR runs entirely in the browser with the engine and English model bundled into the app (~7 MB, loaded only on opt-in) so no student data leaves the browser and the app still makes no external network calls. | DGS | — |
| 2026-09-01 | Can courses from other universities satisfy requirements, and how does the DGS control it? | Students upload up to three external transcripts (Bachelor's, Master's, Ph.D. — all optional; system-generated PDFs only, no scans/OCR, for correctness). Scope: §4.4.1 core knowledge and §5.2 transfer credit only — NOT §4.4.2 category specialization. Bachelor's coursework can satisfy core knowledge ("either at Notre Dame or at their previous institution" has no level limit) but never transfers (§5.2 requires graduate courses taken with graduate student status). Confirmed rulings live in a new ExternalCourses tab of the SAME rules sheet (university + aliases, course id/title, satisfies_core_area, transferable yes/no/blank, optional nd_credits = ND-equivalent pro-rata credits per §5.2, decided_on, notes). A sheet-confirmed core area makes the core row met; transferable=yes still requires the student's formal §5.2 request (DGS recommendation + Graduate School approval); anything without a row shows "not yet reviewed by the DGS" and the student gets a copy-ready email request (the page transmits nothing — FERPA). | DGS | §4.4.1, §5.2 |
| 2026-09-01 | What happens when the live sheet cannot be loaded, and does "alpha" mean the rules may be wrong? | The page no longer falls back to the saved copy silently. A loading card shows progress step by step; if Google does not answer within 15 seconds (or the load fails), the card explains why and suggests reloading the page first; continuing with the app's saved copy is a second-choice button (kept so the tool still works if the sheet is ever unpublished — then it becomes the first choice, since reloading cannot help). And the disclaimer now separates the two: the course rules are accurate — they are exactly the rules the DGS and the Graduate Program Administrator use to determine whether a student's courses satisfy the degree requirements — while what is in alpha is the tool's application of them. | DGS | — |
| 2026-09-01 | Is the public course-rules page official, and how are the rules dated? | Official — the course mappings are made by the DGS with faculty input and are the mapping the DGS and the Graduate Program Administrator use to decide whether a student's courses satisfy the degree requirements; the page says so and is not labelled alpha (the self-check tool still is). The page also notes that not every listed course is currently offered (retired courses hidden by default; active ones run only in some terms). Both pages carry one dated sentence, automatic (no DGS step), worded by the DGS: "The course rules here were last updated on <X>, and are up-to-date as of <Y>" — X is when the sheet's content last changed as recorded by the six-hourly sync Action (Google sends no Last-Modified header for published CSVs, verified 2026-09-01, so the sync's own record is the source), Y is the day the page read the sheet (or the day the fallback copy was saved, when the live fetch failed); while the live sheet is newer than the deployed copy the first half says "were updated after <X>" until the next sync. No course counts on that line. While loading, both pages show a progress card (steps per tab, a bar against the 15-second budget, elapsed seconds). The optional Parameters row `rules_effective_date` overrides it with "Rules effective as of <date>" when the policy date differs from the last edit. | DGS | — |
| 2026-09-01 | Should students get a public list of which courses count, and what should it show? | Yes — `courses.html`, generated live from the Courses tab: course id, title, course type, counts toward MSCSE, counts toward Ph.D., core-knowledge area (§4.4.1), specialization category (§4.4.2), typically offered, and whether the DGS has reviewed and confirmed the row (`dgs_reviewed`, shown as Confirmed / Pending). Overview cards group courses by core area and by category; retired courses are hidden by default; the rule in effect for the current term is shown when a course has several versions. Blank verdicts show as "Not yet decided". | DGS | §3.2, §4.2, §4.4 |
| 2026-09-01 | What contact, feedback and license information does the page show? | A "Who to contact" card at the top right of every page lists: DGS Taeho Jung (tjung@nd.edu) for Ph.D. policies and the graduate program in general; Assistant DGS Aaron Dingler (adingler@nd.edu) for MSCSE policies; Graduate Program Administrator Cari White (csalmons@nd.edu) for logistics, paperwork, processing and everything else. The alpha disclaimer states in bold that every verdict is computed from the DGS's published course rules (linking to the course-rules page) and invites error reports and feedback to the DGS. A note under the transcript-upload button says the PDF upload is untested — FERPA prohibits sharing student records with the AI tools used to build the page, so the parser was written from public information only — and asks for error reports. The footer states the license (© University of Notre Dame du Lac; free non-commercial use; commercial use via the IDEA Center) and links to LICENSE.md and the source. All of it lives in `src/ui/contacts.ts`. | DGS | — |
| 2026-09-01 | Should the page carry an alpha/no-warranty disclaimer, and should handbook mentions link to the official PDF? | Yes. A red banner under the masthead, a footer paragraph, and the copied summary all state: alpha version under testing; informational purposes only; no warranty or guarantee; the final decision on every requirement is at the DGS's discretion. Every mention of the handbook edition links to the official PDF; edition and URL live in one place, `src/ui/handbook.ts`. | DGS | — |
| 2026-08-31 | May the ≤6 CSE-4xxxx credits count *inside* the 24 regular-course credits, despite "at the 60000 level or higher"? | Yes — CSE-4xxxx credits count inside the 24, up to the 6-credit cap. Applies to both programs. | DGS | §3.2, §4.2 |
| 2026-08-31 | Does a non-CSE 40000-level course count toward anything (and can the 4xxxx and non-CSE caps overlap)? | Non-CSE 40000-level courses do not count at all, for either program. The 4xxxx cap therefore covers CSE 4xxxx only, and the two caps can never overlap on one course. | DGS | §3.2, §4.2 |
| 2026-08-31 | What counts toward the 30 (M.S.) / 60 (Ph.D.) total credits? | Only courses that **passed** count toward the totals. Pass = any non-failing final grade (A…D, or S); F/U count nothing; "in progress" counts only provisionally (a row leaning on it shows *in progress*, never *met*). | DGS | §3.2, §4.2 |
| 2026-08-31 | Grade floor for a course to earn credit at all? | Same as above: a failed course (F/U) earns no credit toward any requirement. | DGS | §3.2, §4.2 |
| 2026-08-31 | Students must be able to save progress to a file and import it later. | Added as a first-class requirement: prominent "Save my progress to a file" / "Load a saved file" buttons; versioned JSON that works across devices/browsers. | DGS | — |
| 2026-08-31 | GitHub repository name | "ND-CSE Degree Requirement Progress Checking"; slug `ND-CSE-Degree-Requirement-Progress-Checking` (GitHub disallows spaces; display name kept in README and page title). | DGS | — |
| 2026-08-31 | How do "in progress" (IP) courses count? | IP credits count toward totals and consume caps, but any requirement that needs them shows *in progress*, never *met*. IP never satisfies the §4.4.2 B-floor or the §4.4.1 core-knowledge "pass". | default (DGS-delegated) | §3.2, §4.2, §4.4 |
| 2026-08-31 | Can a course listed under every specialization group (Research Methods, `category_group = any`) fill whichever group the student lacks? | Yes. The student assigns it to one group (dropdown of the five); the engine's matching also uses it automatically for a missing group and suggests a better assignment if the student's pick is suboptimal. One group per taking. | default (DGS-delegated) | §4.4.2 |
| 2026-08-31 | May one course satisfy both a core-knowledge area and a specialization group? | Yes — the handbook allows it explicitly ("Core Knowledge courses and Category Specialization courses may overlap"). | handbook (explicit) | §4.4.2 |
| 2026-08-31 | §3.2 says project = CSE 68902 / thesis direction = CSE 68901, but §3.4(i) says the project runs over 6 credits of CSE 68901. Which mapping is right? | Follow §3.2 (project = 68902, thesis direction = 68901), matching the rules sheet. The app accepts either id toward the 6 research credits, so a mis-registration never costs a student credit. Flag the §3.4(i) typo for the next handbook revision. | default (DGS-delegated) | §3.2 vs §3.4 |
| 2026-08-31 | How are non-CSE courses (absent from the sheet) entered and counted? | Free-text entry; counted provisionally against the 9-credit non-CSE cap with status *needs DGS review* (§3.2/§4.2 require advisor + DGS approval). | default (DGS-delegated) | §3.2, §4.2 |
| 2026-08-31 | Is GPA computed from entered grades or self-entered? | Self-entered cumulative GPA (the transcript is the truth; the app cannot see non-counting courses; §5.2 excludes transfer grades from GPA anyway). Blank → *cannot evaluate*. | default (DGS-delegated) | §2.2, §5.2 |
| 2026-08-31 | How is residency (full-time semesters) determined? | Derived: a term with ≥ 9 entered credits (§2.1.2) is full-time, plus a per-term "I was full-time" override for research-heavy terms. Ph.D. consecutiveness is checked over fall/spring terms; summers are skipped without breaking the run (§4.3). An approved leave currently breaks the run and the report says so — revisit if wrong. | default (DGS-delegated) | §2.1.2, §3.3, §4.3 |
| 2026-08-31 | Grade floor for the §4.4.1 core-knowledge "pass"? | Any passing final grade (consistent with the DGS's totals answer). A transferred course claimed for a core area is always *needs DGS review*. | default (DGS-delegated) | §4.4.1 |
| 2026-08-31 | Seminars (63801/63802) not taken by end of year one — unmet or warning? | The requirement's status reflects only whether both seminars are taken/in progress; past year one without them, a "expected during the first year" warning is added. Driven by the `phd_seminar_courses` parameter, credit-based (2 credits), not hard-coded ids. | default (DGS-delegated) | §4.2 |
| 2026-08-31 | Should the candidacy coursework precondition gate the candidacy row? | No — shown as an informational precondition line under the candidacy requirement, not a gate. | default (DGS-delegated) | §4.5 |
| 2026-08-31 | How is transfer credit entered and limited? | Per-course rows (free-text id, title, credits, grade, term completed, institution). Enforced: grade ≥ B, completed within 5 years prior to admission, caps 6 (unfinished prior M.S.) / 9 to M.S. / 24 to Ph.D. (completed prior degree); a student with no prior graduate program is treated under the 6-credit cap pending DGS review. Every transfer row is *needs DGS review* (§5.2 requires DGS + Graduate School approval). Transferred courses may satisfy §4.4.1 core areas by student self-claim, also *needs DGS review*. | default (DGS-delegated) | §4.2, §5.2 |
| 2026-08-31 | Which sheet schema is authoritative? | The live Google Sheet CSE-Degree-Audit-Rules (17-column Courses tab, two-list Categories tab). `data/README.md` and the sample CSVs are kept in sync with it. | default (DGS-delegated) | — |
| 2026-08-31 | New Parameters rows to add | `ms_total_credits_min=30` (§3.2), `phd_nd_credits_min=9` (§4.2), `phd_transfer_completed_ms_credits_max=24` (§5.2), `ms_transfer_completed_ms_credits_max=9` (§5.2), `transfer_unfinished_ms_credits_max=6` (§5.2), `transfer_min_grade=B` (§5.2), `fulltime_credits_min=9` (§2.1.2). Also `phd_total_credits_min` is citable as §4.2 (resolves the sheet's CONFIRM note). | default (DGS-delegated) | §2.1.2, §3.2, §4.2, §5.2 |
| 2026-08-31 | Do over-cap 4xxxx / non-CSE credits still count toward the 30/60 totals? | No — a credit blocked by a cap "may [not] be used to satisfy the course requirement", and per the DGS totals answer only counting, passed credits reach the totals. The report lists each excluded credit with the cap that excluded it. | default (DGS-delegated) | §3.2, §4.2 |
| 2026-08-31 | Deadline arithmetic conventions | Fall + spring semesters only; semester 1 = entry term (summer entry → semester 1 is the following fall, with a note); deadlines = end of the Nth semester using approximate dates (May 31 / Dec 31), always labeled "(approximate)"; the 18-month research-qualifier clock runs from a nominal entry date (Aug 15 / Jan 10). A deadline past with the milestone missing → *unmet* ("the DGS may extend" noted where §4.4 says so). | default (DGS-delegated) | §3.3, §4.3, §4.4, §4.5 |
| 2026-08-31 | Are §3.5 (Integrated B.S.+M.S.) and §3.6 (Transition to Computing) audited in v1? | No — v1 audits the standard MSCSE and Ph.D. programs; students on those tracks see a clearly-worded "talk to the DGS" note. | default (DGS-delegated) | §3.5, §3.6 |
| 2026-08-31 | Minimum course level for MSCSE regular courses? | Mirrors the Ph.D.: 60000-level or higher, with CSE 4xxxx allowed up to the 6-credit cap as the only exception; 5xxxx courses count nothing. (The handbook is silent; revisit if the real policy differs.) | default (DGS-delegated) | §3.2, §3.6.1 |
| 2026-08-31 | Do S/U grades earn credit and satisfy grade floors? | S = passing credit and satisfies the §4.4.2 "B or higher" floor (S/U courses have no letter grade to compare); U = no credit. | default (DGS-delegated) | §4.2, §4.4.2 |
| 2026-08-31 | Are advisor/DGS approvals self-attested? | Yes — clearly-labeled checkboxes ("my advisor approved…", "the DGS approved…") flip approval-gated rows from *needs DGS review* to *met*; the audit is a self-check, and the footer says approvals must be confirmed with the DGS office. | default (DGS-delegated) | §3.2, §4.2, §5.2 |
| 2026-08-31 | A milestone completed after its deadline — met or flagged? | *Needs DGS review*: "completed after the § deadline — confirm the DGS extended it". | default (DGS-delegated) | §4.4, §4.5 |
| 2026-08-31 | Repeated courses | Credits count once toward 24/30/60 (latest graded attempt kept); duplicate entries warned. For the §4.4.2 "take another course" path, the original grade is replaced only for the category-specialization check; the original course's credits still count toward totals if passed. | default (DGS-delegated) | §4.4.2 |
| 2026-08-31 | Source of the 60-credit Ph.D. total | §4.2 states it directly: "The graduate school requires a total of sixty (60) credits of courses and research for the Ph.D." (The DGS has since updated the sheet's Parameters row to cite §4.2.) | handbook (found) | §4.2 |
| 2026-08-31 | What does the "my advisor approved my plan of study" attestation change? | It is advisory only: while unticked (and any courses are entered), the "Courses needing sign-off" row reminds the student to confirm advisor approval; it does not gate individual courses (the DGS-approval checkboxes do that per §-specific cap). A sheet-listed `dgs_approval` course is cleared by the 4xxxx checkbox when it is 40000-level and by the non-CSE checkbox when it is non-CSE; a CSE course above the 40000 level flagged `dgs_approval` has no checkbox and stays "needs DGS review". | default (DGS-delegated, review 2026-08-31) | §3.2, §4.2 |
| 2026-08-31 | Which retake attempt counts (§4.4.2)? | The last attempt with a **passing** final grade; if none has passed and a retake is underway, the in-progress attempt counts (as in-progress credit); only if every attempt failed does the last failed attempt stand (earning nothing). A later failed attempt never erases an earlier pass. Only Notre Dame courses de-duplicate — transfer ids may collide with ND numbering without being retakes. | default (DGS-delegated, review 2026-08-31) | §4.4.2 |
