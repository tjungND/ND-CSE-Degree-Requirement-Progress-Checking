{
  "name": "nd-cse-degree-audit",
  "private": true,
  "version": "1.0.0",
  "description": "ND CSE Degree Requirement Progress Checking — static self-check audit against the CSE Graduate Studies Handbook §3 (MSCSE) and §4 (Ph.D.)",
  "type": "module",
  "scripts": {
    "dev": "\"./node_modules/.bin/vite\"",
    "build": "\"./node_modules/.bin/tsc\" --noEmit && \"./node_modules/.bin/vite\" build",
    "preview": "\"./node_modules/.bin/vite\" preview",
    "test": "\"./node_modules/.bin/vitest\" run",
    "test:watch": "\"./node_modules/.bin/vitest\"",
    "sync-sheet": "node --experimental-strip-types scripts/sync-sheet.ts"
  },
  "//scripts-note": "Binaries are invoked by relative path, not bare name: a parent folder of this repo has a ':' in its name (FY26-27 (DGS: Taeho Jung)), which corrupts the colon-separated PATH npm builds for scripts. Relative paths work locally and in CI alike.",
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.0"
  }
}
